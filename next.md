# 下一阶段功能计划

更新时间：2026-09-18。状态：**钱包双店铺与每笔充值 10% 返佣已完成本地功能验收，检查与限制见第 15 节；生产发布仍有前置条件**。第 14 节保留 rc.37 升级记录，第 9–13 节保留此前各轮记录，生产发布门槛继续见第 10.5 节。

本次交付为根目录 `next.md`，对应登录协议、公告弹窗、关于页面、文档链接、推荐奖励提现五项需求。第 1–7 节保留原始规划与业务规则（其中阶段 A 需要确认的三项规则按推荐默认值落地，见第 9.1 节），第 9 节记录实际实施、验证证据、未实施事项与剩余风险。

## 0. 实施状态速览

| 需求                            | 状态   | 服务端强制 | 自动化验证                                  | 浏览器验收                      |
| ------------------------------- | ------ | ---------- | ------------------------------------------- | ------------------------------- |
| 登录协议确认（第 3 节）         | 已实施 | 是         | 协议及 OAuth 浏览器绑定三库各 22 子用例通过 | 密码/TOTP 通过，外部入口见 10.5 |
| 公告管理、弹窗与置顶（第 4 节） | 已实施 | 不适用     | 前端及 Go 专项通过                          | 通过                            |
| 关于页面（第 5 节）             | 已实施 | 不适用     | 9 用例通过，补齐归属和隐私摘要              | 通过                            |
| 文档链接配置（第 6 节）         | 已实施 | 是         | 校验、刷新及唯一编辑入口通过                | 通过                            |
| 推荐奖励 48 小时冻结（第 7 节） | 已实施 | 是         | 三库账本、迁移、重试及接口用例通过          | 通过                            |

**结论口径**：rc.37 升级后前端全量 1655/1655，加上新增兑换码粘贴 4 项；后端 44 个测试包、三库迁移与业务专项、24 项真实浏览器和 39 项隔离 HTTP 验收通过。第 9–13 节的数量属于各轮历史结果。修改文件 lint 无 error，全仓 lint 仍有存量问题。生产日志留存、外部身份提供方和运营隐私声明尚未验收，不得据此认为整体生产发布验收完成。

## 1. 范围、基线与优先级

### 初次规划基线

| 项目       | 已核实状态                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------ |
| 仓库与分支 | `D:\newapi`，`main`，跟踪 `fork/main`；开始时工作区干净                                                |
| 基线提交   | `a5fb489a1fe4ae80f8266926fbc65977f6101120`，`feat(redemption): 多选工具栏增加批量删除`                 |
| 交付远端   | `fork` → `https://github.com/lysimportant/forknewapi.git`；`origin` 是上游仓库                         |
| 运行时     | Node `v24.12.0`、Go `go1.26.0 windows/amd64`、仓库本地 Bun `1.4.2`；`go.mod` 要求 Go `1.25.1`          |
| 前端依赖   | 已存在 `web/node_modules`、`web/dist` 和 `web/bun.lock`；本次不安装或升级依赖                          |
| Bun 入口   | 当前 PATH 中没有 `bun`；已验证 `.local-tests/responses-docker/tools/node_modules/bun/bin/bun.exe` 可用 |
| 测试基线   | 规划阶段为文档任务未执行应用测试；实施阶段的测试与三数据库结果见第 9 节                                |
| 任务记录   | 根目录未发现现有 `TODO.md` 或 `next.md`；后续以本文件的阶段清单继续执行                                |

本次文档任务为 **P2、小范围文档变更**。后续实现以 PC Web 为主要验收对象，沿用现有 React、Base UI、Tailwind、Go/Gin 和配置体系，保留当前定制行为、New API 与 QuantumNous 的项目归属信息。

| 后续工作                 | 优先级 | 完成标准                                                               |
| ------------------------ | ------ | ---------------------------------------------------------------------- |
| 登录协议及数据处理口径   | P0     | 登录前主动勾选，所有启用的登录入口遵守同一规则，声明与实际留存行为一致 |
| 推荐奖励逐笔 48 小时限制 | P0     | 服务端逐笔判断，主动操作才到账，并发、重试和历史迁移不重复计款         |
| 公告管理及时间轴弹窗     | P1     | 可增改删、置顶，三个关闭入口行为明确，“今日关闭”跨刷新生效             |
| 文档链接配置入口         | P1     | 系统公告页可设置 URL，保存后 Header 使用新地址                         |
| 关于页面                 | P2     | 有完整介绍与清晰版式，保留已有自定义内容模式                           |

本轮不实施上述功能，不操作生产环境、不迁移真实数据库、不引入外部现金打款系统。若后续确认需要现金提现，先补全该业务方案再实施。其他优化记录为后续项，不夹带无关重构。

## 2. 已核实的现有能力

| 需求     | 当前实现与缺口                                                                                                                                    | 主要入口                                                                                                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 登录协议 | 已有 `LegalConsent` 和登录按钮禁用逻辑，但依赖管理员填写用户协议或隐私政策；未配置时不展示。密码登录请求目前没有同意标记或协议版本                | `web/src/features/auth/components/legal-consent.tsx`、`web/src/features/auth/sign-in/components/user-auth-form.tsx`、`web/src/features/auth/api.ts`、`controller/user.go`        |
| 系统公告 | 有单文本 `Notice`，另有支持增改删的 `console_setting.announcements` 列表；列表已有发布时间和类型，缺少置顶、自动弹窗及时间轴                      | `web/src/features/system-settings/maintenance/notice-section.tsx`、`web/src/features/system-settings/content/announcements-section.tsx`、`setting/console_setting/validation.go` |
| 今日关闭 | 通知 store 已有 `closedUntilDate` 和 `isNoticeClosed()`，尚未接入目标弹窗                                                                         | `web/src/stores/notification-store.ts`                                                                                                                                           |
| 关于页面 | `About` 配置已经支持 Markdown、隔离 HTML、URL iframe；未配置时显示空状态。需要改进单行内容的版式并补充站点介绍                                    | `web/src/features/about/index.tsx`、`web/src/features/system-settings/general/system-info-section.tsx`                                                                           |
| 文档链接 | 已有 `general_setting.docs_link`，当前编辑入口在计费/额度设置；Header 已读取 `/api/status` 的 `docs_link`，保存后的状态刷新白名单尚未包含该配置键 | `web/src/hooks/use-top-nav-links.ts`、`web/src/features/system-settings/general/quota-settings-section.tsx`、`web/src/features/system-settings/hooks/use-update-option.ts`       |
| 推荐奖励 | 邀请注册后累计 `aff_quota`，用户通过“转入余额”手动转到站内钱包；没有逐笔奖励时间、48 小时冻结或现金提现系统                                       | `web/src/features/wallet/components/affiliate-rewards-card.tsx`、`web/src/features/wallet/components/dialogs/transfer-dialog.tsx`、`controller/user.go`、`model/user.go`         |

以上来自代码检查，未读取线上配置，因此不推断当前站点的协议正文、公告内容或实际日志保留设置。

## 3. 登录按钮下方的 API 服务及隐私协议

### 页面与交互

- 在主登录按钮正下方展示未勾选的 checkbox，文案为“我已阅读并同意《API 服务、隐私与使用责任协议》，并承诺依法使用本平台 API 服务。”；点击协议名称在当前页面的 Dialog 阅读完整内容，不跳转 URL，也不改变勾选状态。
- 勾选区域的补充说明为：“本站仅提供大模型 API 接入服务，不出售用户数据或个人信息，不对您的具体用途进行常态化人工监测。您应依法使用服务，并对自身违法使用行为依法承担相应责任。”完整的数据处理范围及必要处理情形见协议正文。
- 默认未勾选，登录按钮允许点击；未同意时点击或回车显示“请先阅读并同意协议后再登录。”，不发送认证请求。备用登录入口执行相同守卫。
- 扩展现有 `LegalConsent`，与已有用户协议/隐私政策合并展示，避免两组重复 checkbox。旧法律文本保留；通过现有站点系统信息中的协议配置补充内容。
- 即使原法律配置为空，本站 API 服务说明仍提供默认内容和强制勾选；状态加载中或失败时不能短暂放开登录。密码登录被关闭时，协议仍在备用登录区域可见。
- 每次新的登录流程重新同意；同一次 OAuth 跳转、Passkey 或二次验证过程保留已确认状态。退出登录后再次登录不能自动沿用上次勾选。
- 注册不要求同意协议，只创建账号、不建立会话；成功返回登录页，协议仍默认未勾选。注册页可阅读相关文档；会直接建立会话的第三方入口先引导至登录页确认协议。

### 登录勾选协议正文

协议名称：**《API 服务、隐私与使用责任协议》**。以下正文供登录页面的协议弹窗或现有协议页面使用；上线前按下节核对实际数据处理行为。

> **一、服务范围**
>
> 本站仅提供大模型 API 接入、调用及请求转发等技术服务。您自行决定服务的具体用途、提交的内容以及生成结果的使用方式，并应对提交、发布或使用的内容进行必要核实。
>
> **二、用户数据与隐私保护**
>
> 本站不出售、出租您的个人信息、账户资料、提问内容或模型回复，不非法收集、使用、披露或向他人提供上述数据，也不将您的提问、模型回复及其中的隐私信息用于模型训练、用户画像或营销。
>
> **三、必要的数据处理**
>
> 本站仅在完成 API 请求及依法履责所需范围内处理您的提问、模型回复和请求数据，不进行与提供服务无关的额外收集。为提供账户认证、用量计费和安全维护，本站会在必要范围内处理、保留账户资料、调用用量、计费记录及安全日志；处理和保留限于实现相关目的所需的范围与期限，法律另有要求的除外。本站依法采取数据保护措施，请勿提交完成请求所不需要的敏感信息。
>
> **四、使用与监测边界**
>
> 本站不主动追踪您的具体使用场景，不对您的提问和模型回复进行常态化人工阅读或监测。为履行适用法律法规、保障服务安全、处置滥用行为或响应有权机关的合法要求，本站可在必要范围内开展安全检测、风险处置及依法配合。上述服务边界不表示本站允许或认可任何违法用途。
>
> **五、合法使用与用户责任**
>
> 您应遵守适用法律法规及相关上游服务规则，确保对提交的数据具有合法使用权限，不得利用本站从事违法活动或侵害他人的合法权益。因您违法使用服务或侵害他人权益而产生的相应法律责任，由您依法承担。本站提供 API 接口不构成对您的具体用途或生成内容的认可、授权或合法性保证。
>
> **六、上游服务与必要处置**
>
> 为完成请求，相关内容会被传输至所调用的上游模型服务，其数据处理和留存规则适用该服务的政策；本站的数据承诺不替代上游服务的承诺。发现违法违规或危及服务安全的行为时，本站可依法依约采取限制调用、暂停或终止相关服务等必要措施。
>
> **七、责任边界与同意**
>
> 本协议关于用户责任的约定，不免除或限制本站依法应承担的个人信息保护、数据安全及其他法定义务和责任。勾选同意表示您已阅读并理解本协议，并承诺依法使用本站服务；如不同意，请勿继续登录和使用。

### 发布前的事实核查

这份协议已补齐“不售卖用户数据”“仅提供 API 服务”“不进行常态化人工监测”和“用户依法承担违法使用责任”的正文。发布前仍须核对数据收集目的、使用及共享情况、留存期限、用途追踪和人工监测方式；账户资料和计费记录不能被错误表述为不存在，上游服务的数据规则也不能由本站代为承诺。

“不监测”在此限定为不主动追踪具体用途、不常态化人工阅读内容；必要的安全检测、计费记录及依法处理须如实说明。“违法使用责任由用户依法承担”针对用户自身行为，不写成“任何责任均由用户承担”或“平台在任何情况下免责”。

实施前须核查普通文本、流式请求、图片/文件、异步视频任务以及异常路径。当前 `model/log.go` 有账号、用量及可选 IP 等字段；`service/error.go` 的异常日志可能包含上游响应片段；`service/task_polling.go` 会保存处理后的任务响应。还需核查 Debug 日志、请求体临时文件、数据库、缓存和反向代理日志。

如发现与协议冲突的额外收集、使用或共享，应先修正对应路径并验证，再发布协议。必要账户/计费记录的字段及保留期限、异步任务处理期是否需要暂存，须在协议定稿时明确；若发现需要保留请求正文的情形，应如实说明用途、范围和期限，不直接声称“完全零收集”或“绝不留存”。本次文案补充不授权删除现存数据或关闭必要安全措施，涉及清理需另行确认。

### 服务端与兼容性

- 登录请求增加同意标记及协议版本，服务端校验当前版本；在已有登录流程中绑定确认状态，最终建立会话前复核。拒绝缺失、未同意、过期版本以及失效/串用的流程。
- 覆盖密码、已启用的 OAuth、微信、Telegram、Passkey 和后续 2FA 流程；OAuth 在发起流程时绑定同意状态，保留第三方回调协议、state 和现有校验。
- 不以浏览器 localStorage 中的布尔值作为服务端同意凭据，不把协议确认当作身份认证，也不降低验证码、限流、MFA、会话轮换及 CSRF 防护。
- 若记录同意凭证，只保留必要的协议版本、时间和关联标识，不记录密码、验证码、令牌或请求正文。
- 强制服务端同意校验会影响直接调用登录接口的旧客户端，实施前列出调用方并同步升级；不能为了兼容缺少字段的请求而保留绕过入口。API Key 的模型调用不应被新增为交互式登录流程。

### 验收

- [x] 默认未勾选；未同意时点击登录或回车有 Message 提醒且不发送认证请求；同意后才能登录，取消勾选后再次阻止。（`sign-in/__tests__/legal-consent.test.tsx`，第 12 节实际注册登录验收）
- [x] 协议在 Dialog 内阅读，关闭恢复焦点，加载/失败可重试；注册无需勾选，成功返回登录且未自动同意。（`auth/components/__tests__/legal-consent.test.tsx`、`sign-up/__tests__/legal-consent.test.tsx`；独立协议 URL 保留兼容）
- [ ] 所有启用的登录方式及二次验证均通过；直接请求、过期版本、回调串用和重放被拒绝。（本地密码/TOTP 浏览器及三库协议、OAuth 同浏览器绑定测试通过；生产已启用的 OAuth/微信/Telegram/Passkey 配置与真实回调仍需部署证据，见 10.5）
- [ ] 会话轮换、验证码、限流和敏感日志控制通过针对性回归；数据使用、共享和留存核查有可复核结果。（代码层清单和定向回归已完成，见 10.4；生产保留期限、日志访问权限、反向代理与通知共享配置未核实）
- [x] 登录勾选摘要、完整协议和关于页统一说明不售卖数据、服务与监测边界及用户使用责任；必要数据处理和平台法定义务没有被摘要省略成绝对免责。（正文与摘要已按此口径内置，见第 3 节）

## 4. 公告管理、时间轴弹窗与置顶

### 管理入口与数据

将管理入口集中到 **系统设置 → 站点 → 系统公告**（现有 `/system-settings/site/notice`），页面包含“公告列表”和“文档链接”两个区域。

- 复用已有 `AnnouncementsSection`，提供新增、编辑、删除、发布时间、类型、正文、额外说明、置顶/取消置顶及展示开关。
- 继续使用 `console_setting.announcements` 和 `console_setting.announcements_enabled`，不建立第三套公告数据。
- 保留旧字段 `id`、`content`、`publishDate`、`type`、`extra`，仅补充必要的 `pinned`。旧数据缺少 `pinned` 时按 `false` 读取；前后端均校验类型。
- 默认支持多条置顶。排序为“置顶优先 → 各组发布时间倒序 → 时间相同时保持稳定顺序”；弹窗、通知列表和仪表盘保持一致。
- 保留现有限制：最多 100 条，正文 500 字，额外说明 100 字，时间使用 RFC3339。若以后确需长公告再单独调整限制，不在本轮隐式放宽。
- 添加/编辑弹窗确认、置顶和删除确认即写入服务端，无需再点“保存设置”；只有收到成功才显示完成，失败保留编辑内容和旧列表。发布时间用于展示，不增加定时发布、附件或草稿审批系统。
- 原内容设置页保留导航到统一编辑入口。旧 `Notice` 文本保留在兼容区域，继续维持已有读取契约；如管理员选择迁入列表，必须保留原文并避免重复展示，不自动覆盖或删除。
- 管理权限沿用 `/api/option/` 的 `RootAuth`；公开读取接口仅返回可公开的公告数据。保存失败保留草稿，保存成功刷新公告和 status 缓存。

### 弹窗布局

复用 `web/src/components/dialog.tsx` 的关闭按钮、内容滚动和 footer 插槽，组合为公告业务组件。

```text
┌──────────────────────────────────────────────┐
│ 系统公告                                  × │
│                                              │
│ ● 置顶 · 2026-09-16 10:00                     │
│ │ 公告内容……                                 │
│ │                                            │
│ ● 2026-09-15 18:30                            │
│ │ 公告内容……                                 │
│                                              │
│                    [关闭公告] [今日关闭]       │
└──────────────────────────────────────────────┘
```

- 右上角为关闭按钮；右下角两个按钮固定为“关闭公告”“今日关闭”。正文沿垂直时间轴展示时间、类型/置顶标识和内容。
- 桌面宽度建议约 680–760 px、最大高度约为视口的 80%；长内容仅在正文区域滚动，页头和两个底部按钮持续可见。
- 沿用现有日期格式化、`RichContent` 和主题组件；正文安全渲染，不允许注入脚本。无须新增通用 Dialog 或引入时间轴库。
- 弹窗可查看全部配置公告，不能直接沿用通知 hook 的“前 20 条”截断而无入口查看其余记录。

### 关闭及展示规则（建议默认值）

| 操作或场景                           | 计划行为                                                                         |
| ------------------------------------ | -------------------------------------------------------------------------------- |
| 自动弹出                             | 首页、关于页等公共页面或控制台首次进入时，仅对已启用且非空的系统公告列表弹出。旧 Notice 只在通知面板展示，登录页不自动遮挡登录流程 |
| 右上角 × / 关闭公告 / Esc / 点击遮罩 | 关闭本次；当前标签页的本次页面会话不因路由切换反复弹出，刷新或下次进入可再次弹出 |
| 今日关闭                             | 持久化至当前浏览器本地日期结束，刷新和重新进入仍不自动弹出；次日首次进入恢复     |
| 当天新增或置顶公告                   | 尊重已选择的“今日关闭”，不强制打断用户                                           |
| 手动查看                             | 通知入口提供“查看全部公告”，始终可以重新打开，不受今日关闭限制                   |
| 无公告、展示关闭或读取失败           | 不弹空 Dialog；失败保留正常页面功能及可见重试入口                                |
| 本地存储不可用                       | 降级为内存中的本次关闭；明确提示今日关闭无法跨刷新保存，不影响使用               |

复用现有 `notification-store`，只持久化必要日期/标识；如调整结构，兼容既有存储。该设置按浏览器生效，不新增跨设备账户偏好表。“已读”与“今日关闭”分别处理，不将关闭动作等同于阅读所有公告。

### 验收

- [x] 管理员可增改删并切换置顶，保存后公开展示同步，旧公告无数据丢失。（`announcements-section.test.tsx`；旧数据缺少 `pinned` 按 false 读取并有 Go 用例覆盖）
- [x] 时间轴排序正确；长文本、100 条记录、深浅主题下布局正常。（Chromium 151，1440×900 / 1280×720，100 条全部可达，720px 宽、80vh 上限，长链接换行，见 10.3）
- [x] 右上及右下按钮符合要求；Esc、遮罩、焦点圈定与关闭后的焦点恢复正常。（真实浏览器操作通过，等待关闭动画结束后核对焦点恢复；页头和底部按钮在正文滚动时保持可见）
- [x] 普通关闭、今日关闭、刷新、跨日、新增公告及手动重开符合上表。（`notification-store.test.ts`、`use-notifications.test.tsx`）
- [x] 存储失败、请求失败、空列表、非法输入均有合理行为；普通用户无法改配置。（存储降级与空列表有测试；管理接口沿用 `RootAuth`）

## 5. 关于页面美化与推荐内容

### 版式

采用与现有站点一致的居中介绍页：顶部标题区、三张服务卡片、三步接入流程、服务与隐私说明、帮助入口。复用 `PublicLayout`、`Card`、`RichContent`、现有按钮和主题变量。

| 区域       | 推荐内容                                                                                       |
| ---------- | ---------------------------------------------------------------------------------------------- |
| 标题区     | 现有站点名称；副标题“面向开发者的 API 接入服务”；按钮“查看接入文档”“浏览模型”                  |
| 服务卡片一 | **统一 API 接入**：通过统一入口接入站点已开放的模型，具体协议与可用范围以文档及模型列表为准    |
| 服务卡片二 | **账户与用量管理**：管理 API Key，查看余额、调用记录与用量统计                                 |
| 服务卡片三 | **清晰的接入指引**：通过文档完成配置，通过公告了解维护及服务变更                               |
| 接入步骤   | 注册/登录账户 → 创建 API Key → 按文档配置并发起请求；若站点关闭注册，提示联系管理员获取账户    |
| 服务边界   | 提供 API 接入与转发服务；可用模型、价格和限制以站内实际配置为准                                |
| 隐私说明   | 摘要引用第 3 节：不售卖用户数据、仅提供 API 服务、监测边界及用户依法使用责任，并链接到完整协议 |
| 帮助区域   | 配置后的文档链接、系统公告、已配置的支持联系方式；没有联系方式则隐藏该项                       |
| 页底       | 保留现有 New API、QuantumNous、许可证和项目归属信息                                            |

不编造在线用户数、SLA、响应速度、认证资质或固定价格，也不增加虚构客服地址。

### 现有内容兼容

- 继续由 `About` 配置和 `GET /api/about` 提供管理员内容，入口仍在“站点 → 系统信息”。
- 空内容使用完整默认介绍；普通文本/Markdown 在新页面中作为“站点介绍”正文展示，使现有一行文本也有合理版式。
- 现有自定义 HTML 和 URL iframe 保留原有隔离及 sandbox 模式，不破坏管理员定制页面，也不把推荐模板覆盖到配置中。
- 文档和模型按钮遵守既有导航开关及访问权限；加载、错误状态复用项目组件，不把请求失败误认为管理员未配置。
- 以 PC Web 的 1440×900、1280×720 及深浅主题为主要视觉验收；较窄窗口检查不溢出，不扩展为独立手机端改版。

### 验收

- [x] 默认页面具有完整内容层级；一行文本和长 Markdown 都有清晰展示。（`about/__tests__/page.test.tsx`，含单行文本与长 Markdown 用例）
- [x] HTML、URL 自定义模式、主题和现有归属信息不回退。（同一测试覆盖隔离 HTML 与 URL iframe；归属信息在每个分支都断言存在）
- [x] 文档/模型/公告入口可用，没有虚构信息或不实隐私承诺。（入口复用现有导航配置；正文只引用第 3 节已确认的口径，未添加人数、SLA、价格或资质）
- [x] 键盘、加载/错误状态、正文和长链接溢出检查通过。（9 个组件用例通过；真实浏览器检查长 Markdown、长 URL、900px 窗口及深浅主题，无横向溢出）

## 6. Header 文档链接可配置

- 在“系统公告”页面增加“文档链接”设置区域，复用现有 `general_setting.docs_link`，保留已保存值；不新增含义重复的配置键。
- 原计费/额度设置中的编辑入口移至此处，原位置可保留导航提示，避免两个表单各维护一份草稿。
- 沿用 `PUT /api/option/` 保存，`GET /api/status` 返回 `docs_link`，登录前后 Header 共用 `useTopNavLinks`。
- 前后端校验完整 `http://` 或 `https://` URL，拒绝 `javascript:`、`data:`、无效地址和带用户名/密码的 URL；仅存储链接，不由服务端抓取该地址。
- 保存成功后立即失效 status 查询和旧本地缓存；Header 下一次点击使用新地址。后台保存失败时不显示成功或丢失输入。
- 继续以新标签页打开外部链接，保留 `noopener noreferrer`。导航显示开关仍由现有 Header 配置控制。
- 清空时建议恢复现有默认文档地址 `https://docs.newapi.pro`，界面提示“留空使用默认文档”。当前 Header 的空值回退是 `/docs`，但路由目录未发现对应页面，因此实施时需统一空值行为并验证默认地址有效，避免死链接。
- 首页文档按钮也使用同一 `docs_link`，关于页的新文档按钮同样复用；在设置说明中告知这是共享地址。

验收：保存后无需强制刷新即可跳到目标 URL；刷新后配置保持；登录前后 Header 一致；旧值无丢失；非法 URL 被后端拒绝；无权限用户不可修改；空值行为有回归覆盖。

## 7. 推荐计划：逐笔等待 48 小时后手动提现

### 需要先明确“提现”的含义

当前系统只有邀请奖励转入**站内可用余额**的功能。本计划建议先按这一含义实施，并在弹窗中明确“提取至站内可用余额”。

如果期望提现到支付宝、微信或银行卡，需要补充收款方式、提现申请、审核、实际打款、手续费、失败/退款和对账规则；在用户确定前，不把站内余额划转当作现金提现交付。

### 用户可见行为

- 在钱包推荐计划保留邀请码、邀请链接及复制功能，增加明确的“提现”按钮、冻结金额、可提现金额和奖励明细。
- 每笔奖励显示金额、产生时间、可提现时间以及“冻结中 / 可提现 / 已提现”状态；保留部分提现时还需显示剩余金额。
- **每一笔奖励从自身成功产生的服务端时间起独立计算 48 小时**：`available_at = created_at + 172800 秒`，不是注册账号统一计时，也不是第一次提现后统一计时。
- 未到期金额不允许提现；到期仅改变资格，不自动划转。刷新、登录、页面停留、定时任务均不得触发自动提现。
- 主按钮建议提取用户选择金额范围内的已到期奖励，默认填入全部可提金额；明细逐笔计时，扣减顺序固定为到期时间、记录 ID 升序。
- 没有可提金额时按钮禁用并显示最近一笔的可提现时间。倒计时仅作展示，前端根据服务端返回的当前时间校准，每次提交仍由后端重新判定。
- 复用现有 `TransferDialog`，显示提现去向、金额、最低额度说明及确认按钮；提交中防重复点击，成功刷新余额/明细，失败保留可重试状态。

示例（北京时间展示，底层使用 UTC 时间戳）：

| 奖励 | 产生时间         | 可以手动提现的最早时间 | 9 月 18 日 10:30 的状态  |
| ---- | ---------------- | ---------------------- | ------------------------ |
| A    | 9 月 16 日 10:00 | 9 月 18 日 10:00       | 可提现，但不会自动到账   |
| B    | 9 月 16 日 12:00 | 9 月 18 日 12:00       | 冻结中；A 的提现不影响 B |

### 账本、接口与原子性

- 当前 `User.AffQuota`、`AffHistoryQuota` 只有汇总，无法支持逐笔时间。新增邀请奖励账本，至少记录：归属用户、唯一奖励来源、奖励类型、整数额度、剩余额度、产生时间、可提时间及提现记录关联。
- 奖励来源使用被邀请用户和奖励类型等稳定标识去重；普通注册、OAuth 注册及回调重试只能发放一次。邀请人奖励 `QuotaForInviter` 纳入冻结，受邀人注册赠送 `QuotaForInvitee` 不顺带改变。
- 奖励写入、邀请计数及汇总余额必须保持原子一致，失败显式返回或进入可恢复流程，不能继续忽略发奖错误；兼容现有普通/OAuth 用户创建事务边界。
- 新增读取当前用户奖励汇总/分页明细的能力，返回冻结额、可提额、服务端时间和逐笔时间；接口不能读取他人奖励。具体路由在实现前与现有 wallet API 一并定稿。
- 保留 `POST /api/user/aff_transfer` 路径，接入相同的成熟奖励校验与扣账逻辑；不能让旧接口绕过新按钮的 48 小时限制。为保证提交重试安全，写请求需携带幂等请求标识；缺少该标识的旧客户端应收到明确的升级提示且不扣账，并在接口变更说明中记录。
- 每次提现在同一事务内校验用户归属、到期时间、剩余额度、最低额度、钱包上限，扣减对应奖励并增加钱包余额，写入唯一提现请求标识及分配明细。重试应返回同一结果，不能重复记款。
- 沿用整数 quota 和项目钱包上限 `common.MaxWalletQuota`；拒绝零、负数、超额和越界值。保留付款条款检查、身份校验及关键操作限流。
- 通过 `lockForUpdate(tx)` 兼容 MySQL/PostgreSQL；SQLite 不支持该锁语法，必须结合条件更新、受影响行数和事务冲突处理验证一致性，不能把用户行锁当作全部防重措施。
- 事务提交后同步或失效余额缓存；记录不含认证凭据的提现审计。若日志使用独立数据库，不假设主库交易和日志库具有跨库事务，需验证失败记录/补记方式不会引发重新入账。

### 历史数据与现有规则

1. **历史余额**：旧汇总不能可靠恢复每笔产生时间。推荐将迁移前剩余 `aff_quota` 生成唯一“历史结转”记录并保持可提，之后的新奖励逐笔冻结 48 小时。该做法是明确的历史例外，需要实施前确认；如要求所有旧余额也等待 48 小时，应明确从迁移时刻重新起算，不能伪造历史逐笔时间。
2. **最低额度**：目前模型层最低转入量为 `common.QuotaPerUnit`，默认 500000 quota（一个基础货币单位）。推荐保留并明确提示；48 小时到期不等于自动取消最低额，也不能按显示币种误算门槛。
3. **部分提现**：推荐保留旧的自选金额行为，对已成熟记录按固定顺序扣减；明细允许部分剩余，未到期金额不被挪用。若决定改成只能提现整笔或全额，应先明确旧客户端兼容策略。
4. **可核对性**：迁移前后剩余推荐余额一致，新账本剩余额度合计与汇总对应，累计历史奖励不因迁移或提现重复增加；迁移唯一键确保重复启动不生成第二笔结转。

### 迁移、备份与回滚

这是涉及钱包、数据格式和公开接口的高风险实现阶段，实施前先确认以上三项业务规则，备份用户余额、奖励汇总、相关配置及独立日志库，并保存对应应用版本。

- 采用新增账本的兼容迁移，先在隔离数据上验证，再按授权的维护窗口迁移；不删除旧汇总字段，不重复给钱包加钱。
- 对真实 SQLite、MySQL、PostgreSQL 分别执行新建数据库、代表性旧版本升级、重复两次启动、历史结转与并发提现验证；基线同时覆盖上游最新发布版本和当前 fork 的实际结构。
- 记录准确数据库版本、命令、结果和前后余额对账；涉及版本特定行为时加测项目最低支持版本，日志库独立配置也要覆盖。
- 回滚先暂停奖励/提现写入并对账。旧程序的 `/aff_transfer` 不识别冻结账本，不能直接退回旧二进制继续放款；优先使用保留冻结校验的兼容修复版本。恢复旧备份必须处理迁移后的真实交易，不能抹掉已发生的资金变动。

### 验收

- [x] 47:59:59 拒绝，48:00:00 允许；改变浏览器时间不能提前提现。（`TestInviteRewardFreezeBoundary`，三数据库；判据始终是服务端时间）
- [x] 多笔不同时间分别解冻；A 提现不影响 B；到期后不点击不会到账。（`TestInviteRewardWithdrawAllocatesPerReward`，三数据库）
- [x] 普通注册、OAuth 注册及重复回调只产生一份奖励，受邀人赠送规则保持一致。（`TestInviteRewardGrantIsIdempotentPerSource`，三数据库）
- [x] 连点、网络重试、相同请求重放和两个会话并发只产生一次有效入账；失败整笔回滚。（`TestInviteRewardWithdrawIdempotentAndRejectsInvalidInput`、`TestInviteRewardConcurrentWithdrawCreditsOnce`，三数据库）
- [x] 旧接口不能绕过等待期；越权、负数、零、超可提额、最低额和钱包上限都有覆盖。（原 `/api/user/aff_transfer` 路径即新实现，冻结校验不可绕过；上条用例含全部分支）
- [x] 部分提现、刷新后明细/余额/缓存一致；历史结转幂等且可对账。（提交后同步缓存增量；`TestInitializeInviteRewardLedgerIsIdempotent` 含账本-汇总对账）
- [x] 真实三数据库及独立日志库所需验证通过；环境变量缺失导致的跳过不算通过。（三引擎矩阵已通过且未跳过；提现接口在 SQLite/MySQL/PostgreSQL × 共享/独立日志库 六种组合下均通过，见第 9.3 节）

## 8. 实施顺序、检查与交付

### 阶段清单

- [x] 完成五项需求及现有实现调查，核实可复用组件、配置和接口。
- [x] 形成此计划，区分建议、兼容边界和未确认的业务规则。
- [x] 按用户补充细化登录勾选文案与完整协议，写明数据不售卖、API 服务范围、监测边界及违法使用责任。
- [x] 阶段 A：确定提现去向、历史余额规则、最低额度/部分提现规则及可发布的协议表述；补采当前测试和数据处理基线。
- [x] 阶段 B：整合系统公告设置入口、复用文档 URL 配置并修复缓存刷新，完成置顶与时间轴弹窗。
- [x] 阶段 C：核对实际数据处理与协议正文，修正与最终声明冲突的路径，再实现登录协议确认；随后完成引用同一说明的关于页面。
- [x] 阶段 D：在确定的提现方案下完成账本、幂等发奖、成熟奖励提现、历史迁移及钱包界面。
- [x] 阶段 E：完成本地专项回归、三数据库验证、桌面操作冒烟及最终差异审查，更新本文件的证据和剩余事项。
  - 最新验收见第 10 节；本地验收不包含生产配置确认、真实外部身份提供方或真实数据库上线迁移。第 10.5 节发布门槛继续保持未完成。

P0 表示必须守住的正确性和发布门槛；界面可以分阶段完成，不能在 P0 验证缺失时把整体需求标为完成。

每个阶段记录修改文件、最后成功命令、结果、阻塞项和下一步。若并行开展，公告/关于页、认证、钱包按模块分工，共享的状态类型、路由、配置保存 hook 和翻译文件由主任务串行整合，避免并发覆盖。

### 实施后的验证要求

- 前端：先复用项目业务组件；补充受影响模块 `__tests__/` 下的用户行为测试，覆盖公告关闭/跨日、设置保存刷新、协议禁用/绕过、关于页内容模式、奖励冻结/提交反馈。
- 国际化：实施文案前读取 `i18n-translate` 技能，使用英文源字符串键，补齐 en、zh、zh-TW、fr、ja、ru、vi；管理员输入的公告原文不自动翻译或改写。
- 前端命令：在 `web/` 执行 `bun run test`（先相关目录再完整范围）、`bun run typecheck`、受影响文件的 oxlint/格式检查、`bun run build`；真实脚本以 `web/package.json` 为准。复用已验证的本地 Bun 入口，缺依赖时才按锁文件安装。
- 后端：优先扩展现有合适测试；推荐奖励若需新文件，将主要业务用例集中在一个文件，使用 `require`/`assert`。执行相关测试及 `go test ./...`、`go vet ./...`、`go build ./...`；先确保所需 `web/dist` 存在。
- 数据库：凡公告数据格式、配置持久化、奖励账本或迁移受影响，按仓库规范在真实 SQLite/MySQL/PostgreSQL 上验证对应存取/迁移路径；新增账本还需新库、升级、重复启动和并发矩阵。不得以 mock、单 SQLite 或构建通过代替。
- 桌面冒烟：登录确认 → 控制台公告 → 今日关闭/重开；管理员增改公告/置顶 → 用户查看；修改文档 URL → Header 跳转；关于页；奖励从冻结到手动提现。每条流程至少实际操作一次，检查浏览器控制台错误和深浅主题。
- 若触及 `relaykit/`，额外执行独立模块的 `GOWORK=off go build ./...`；未涉及则不扩展修改范围。
- 提交前检查完整 diff、Git 状态、凭据/调试输出及生成噪声。后续实现属于跨模块、含钱包迁移的大变更，验证完成后按仓库规则提交中文 Conventional Commit、创建中文说明的 annotated Tag 并推送 fork 对应分支及 Tag；生产发布另行确认。

### 认证设计参考

本次核查到的最新稳定 ASVS 为 **5.0.0**（GitHub 的 `latest` 滚动构建不作为稳定版本）。实施时重新核对最新稳定版，根据实际改动映射适用条目 ID，并在验证记录中保留引用和结果；本计划不构成合规认证。

- [OWASP ASVS 5.0.0 稳定发布](https://github.com/OWASP/ASVS/releases/tag/v5.0.0_release)
- [Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)：统一失败信息、认证保护及不含凭据的审计。
- [Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)：会话轮换、失效和服务端状态校验。
- [CSRF Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)：协议确认、配置写入和提现等状态变更请求的服务端防护。
- [OAuth 2.0 Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/OAuth2_Cheat_Sheet.html)：在改动 OAuth 发起与回调流程前核对 state、PKCE 等适用控制。

### 当前检查点

实施阶段已完成，当前检查点见第 9 节。

2026-09-16 协议补充基线：`68e1ce6da6a541770787382639e8f2d2001dd77c`，`main` 跟踪 `fork/main`，开始时工作区干净。本次仅更新第 3 节协议及对应摘要、验收和检查点，登录界面和实际数据处理行为仍待实施。

文档验收：`oxfmt --check next.md` 通过，23 处本地文件引用均存在，文档差异的空白检查通过，常见凭据模式检查无命中。应用 lint、类型检查、测试、构建和数据库验证不属于本次文档交付结果，需在后续实现时执行。

## 9. 实施记录与验收证据

实施基线：`132c9a6ea`（`docs(privacy): 补充登录隐私与使用责任协议`），`main` 跟踪 `fork/main`，开始时工作区干净。以下只记录实际执行过的命令与结果。

### 9.1 阶段 A：已确定的业务规则

第 7 节要求实施前确认三项规则，本次按推荐默认值落地：

| 规则     | 采用口径                                                                                          |
| -------- | ------------------------------------------------------------------------------------------------- |
| 提现去向 | 仅“提取至站内可用余额”，弹窗明确标注去向；未引入任何现金打款、收款方式或审核流程                  |
| 历史余额 | 迁移前剩余 `aff_quota` 生成唯一“历史结转”记录（`source_key = history:<用户ID>`）并保持可提        |
| 最低额度 | 沿用 `common.QuotaPerUnit`（默认 500000 quota）；48 小时到期不等于取消最低额，仍按整数 quota 判定 |
| 部分提现 | 保留自选金额，按“到期时间 → 记录 ID 升序”逐笔扣减；未到期金额不被挪用                             |
| 协议表述 | 采用第 3 节正文作为内置默认协议，管理员配置可覆盖；摘要与正文口径一致                             |

冻结时长由新配置项 `InviteRewardFreezeHours` 控制，默认 48；设为 0 表示取消冻结期（历史结转不受影响）。该配置项同时写入 `common.OptionMap` 与 `model/option.go` 的加载/校验分支，非法值被拒绝并记录错误，保持原值。

### 9.2 阶段 B–D：实际改动

**登录协议确认（第 3 节）**

- 新增 `setting/system_setting/api_service_agreement.go` 内置协议正文，`legal.api_service_agreement` 可覆盖；`CurrentLegalConsentVersion = "2026-09-16"` 为生效版本，修订正文必须同步递增。
- `GET /api/status` 新增 `legal_consent: {version, required}`；新增公开接口 `GET /api/api-service-agreement`。
- 服务端强制入口：`POST /api/user/login`、`POST /api/user/register`（新增 `consent` / `consent_version` 字段）、`POST /api/oauth/state`（仅 `intent=login`，版本绑定到服务端 auth flow）、`GET /api/oauth/wechat`（`consent_version` 查询参数）、`POST /api/user/passkey/login/begin`（版本绑定到 passkey 流程，finish 复核）。二次验证在 `CompleteLoginVerification` 建立会话前复核流程内记录的版本。
- 错误以业务错误返回并带明确 code：`legal_consent_required` / `legal_consent_outdated`，同时返回当前版本，且发生在凭据验证之后，不额外泄露用户名是否存在。
- 前端：`LegalConsent` 扩展为始终渲染且默认未勾选；密码、注册、Passkey、OAuth、Telegram、微信、2FA 入口受同一状态控制；协议正文可阅读；新增 `/api-service-agreement` 页面。

**公告、置顶与时间轴弹窗（第 4 节）**

- 后端 `setting/console_setting/validation.go`：`pinned` 按布尔校验；`GetAnnouncements()` 排序改为置顶优先 → 发布时间倒序 → 稳定；旧数据缺少 `pinned` 按 `false` 读取。
- 管理入口统一到 `/system-settings/site/notice`：公告列表（含置顶列与置顶开关）+ 文档链接 + 兼容的旧 `Notice` 文本区。
- 时间轴弹窗复用 `@/components/dialog`，footer 固定为「关闭公告」「今日关闭」；通知列表、仪表盘与弹窗使用同一排序。
- `notification-store` 接入 `closedUntilDate`（今日关闭，跨刷新生效）与会话级关闭；存储不可用时降级为内存关闭且不抛错。

**关于页面（第 5 节）**

- 重建为标题区、三张服务卡片、三步接入流程、服务与隐私说明、帮助入口；管理员内容三模式（Markdown / 隔离 HTML / URL iframe）保持原行为。
- 修复两处既有缺陷：`/api/about` 失败不再静默显示为空状态，改为可重试的错误态；New API / QuantumNous / One API / AGPL 归属信息从空状态移到页面，任何内容模式下都保留。

**文档链接（第 6 节）**

- 「系统公告」页新增文档链接区域，复用 `general_setting.docs_link`；前后端同时校验完整 `http(s)` URL 并拒绝 `javascript:`、`data:` 与带用户名/密码的地址。
- `general_setting.docs_link` 加入 status 刷新白名单，保存后立即失效 `['status']` 与本地缓存；空值回退统一为 `https://docs.newapi.pro`（原 `/docs` 无对应路由，是死链接）。

**推荐奖励账本与冻结提现（第 7 节）**

- 新增 `model/invite_reward.go` 与两张表：`invite_rewards`（归属用户、唯一来源、类型、额度、剩余额度、产生时间、可提时间）、`invite_reward_withdrawals`（用户、幂等键、额度、分配明细 JSON）。已在 `model/main.go` 的 `AutoMigrate` 注册，并在启动迁移中执行幂等历史结转 `InitializeInviteRewardLedger()`。
- 发放：`GrantInviteReward` 在事务内按来源去重并同步 `aff_count` / `aff_quota` / `aff_history`；受邀人赠送同样按来源去重。注册与 OAuth 注册共用 `finalizeCreatedUser`，发奖失败显式记录错误而不是静默忽略。
- 提现：`POST /api/user/aff_transfer` 保留原路径，新增 `idempotency_key`；服务端在事务内校验归属、到期时间、剩余额度、最低额与 `common.MaxWalletQuota`，按固定顺序逐笔条件扣减，重复提交返回首次结果，缺少幂等键返回 `invite_reward_idempotency_key_missing`。
- 新增 `GET /api/user/aff/rewards`，并在 `GET /api/user/self` 响应中附带 `referral_rewards` 汇总，钱包页无需新增请求即可展示冻结/可提金额、逐笔状态与可提时间。
- 前端：推荐卡片显示可提/冻结/累计/邀请数，新增奖励明细时间轴弹窗；提现弹窗显示去向、冻结金额、默认填入全部可提金额，并按每次提交生成幂等键。

### 9.3 验证命令与结果

运行环境：Go `go1.26.0 windows/amd64`（`GOWORK=off`）；Bun `1.4.2`（仓库本地入口）；SQLite `3.50.4`；MySQL `8.0.46`（容器 `rc35-verification-mysql`，本机回环 `33358`）；PostgreSQL `16.15`（容器 `rc35-verification-postgres`，本机回环 `33356`）。

**后端**

| 命令                                                                                                                                                             | 结果                                                              |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| `go build ./...`                                                                                                                                                 | 通过（exit 0）                                                    |
| `go vet ./...`                                                                                                                                                   | 通过，无输出                                                      |
| `go test ./controller/ ./model/ ./service/... -count=1`                                                                                                          | 全部 `ok`（controller 144.5s、model 11.3s）                       |
| `go test ./model/ -run 'TestInviteReward\|TestInitializeInviteRewardLedger' -count=1 -v`（三数据库 DSN）                                                         | 7 个用例 × SQLite/MySQL/PostgreSQL 全 PASS                        |
| `go test ./controller/ -run 'TestLoginConsentEnforcement' -count=1`                                                                                              | 通过，含 4 个密码登录拒绝子用例与注册拒绝子用例                   |
| `TEST_MANAGE_USER_SEPARATE_LOG_DB=1 TEST_MANAGE_USER_DIALECT=<engine> go test ./controller/ -run 'TestTransferAffQuotaEnforcesFreezeAndIdempotency' -count=1 -v` | 通过；六种组合（SQLite/MySQL/PostgreSQL × 共享/独立日志库），见下 |

**独立日志库（`LOG_SQL_DSN` 指向另一个库）路径**：提现用例在 `TEST_MANAGE_USER_SEPARATE_LOG_DB=1` 下逐个引擎执行，主库与日志库分别建库，实际结果：

| 引擎       | 版本                                     | 共享日志库       | 独立日志库                     |
| ---------- | ---------------------------------------- | ---------------- | ------------------------------ |
| SQLite     | 3.50.4                                   | PASS（1 条日志） | PASS（log DB 1 条、主库 0 条） |
| MySQL      | 8.0.46                                   | PASS（1 条日志） | PASS（log DB 1 条、主库 0 条） |
| PostgreSQL | 16.15 (x86_64-pc-linux-musl, gcc 15.2.0) | PASS（1 条日志） | PASS（log DB 1 条、主库 0 条） |

用例同时断言 `model.LOG_DB` 与主库在两种配置下分别是「同一连接」和「不同连接」，避免出现日志缺失或同一条日志写两处被误判为通过。

奖励账本覆盖：逐笔冻结 47:59:59 拒绝 / 48:00:00 允许、多笔分别解冻与固定扣减顺序、普通注册与重复回调只发一份、受邀人赠送不顺带改变冻结账本、缺幂等键、零/负数、超可提额、钱包上限、重复 requestId 重放、两个会话并发只入账一次、历史结转幂等（重复执行只生成一笔）与账本-汇总对账、以及真实三库上的表结构第二次迁移零 DDL。

**前端**

| 命令                              | 结果                                         |
| --------------------------------- | -------------------------------------------- |
| `bun run typecheck`               | 通过（exit 0）                               |
| `bun run build`                   | 通过，`web/dist` 已重建（Go 服务嵌入该目录） |
| oxlint（本次涉及文件）            | 0 error                                      |
| `bun run test`（全量 108 个文件） | 813 通过 / 17 失败，失败均为既有问题，见 9.4 |

新增前端用例：登录/注册协议禁用与绕过（11）、关于页内容模式与错误态（7）、公告排序/弹窗/关闭语义（24）、文档链接校验（4）、奖励提现弹窗与幂等键（9）。

**交付前的合并核对**（全部提交完成后重跑，确认证据与最终代码一致）：

| 命令                                                                                                                     | 结果                                           |
| ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------- |
| `go test ./model/ -run 'TestInviteReward\|TestInitializeInviteRewardLedger' -count=1 -v`（三数据库 DSN）                 | 顶层 8 PASS / 子用例 21 PASS / 0 FAIL / 0 SKIP |
| `go test ./controller/ -run 'TestLoginConsentEnforcement\|TestTransferAffQuotaEnforcesFreezeAndIdempotency' -count=1 -v` | 顶层 2 PASS / 子用例 12 PASS / 0 FAIL          |
| `vitest run`（本次新增前端用例所在目录）                                                                                 | 14 个测试文件 / 58 用例全部通过                |

### 9.4 上一轮未执行与既有问题（历史记录）

下列为 2026-09-16 实施阶段原始结果；本轮解决状态以第 10 节为准。

- **未在真实浏览器验收**：1440×900 / 1280×720、深浅主题、窄窗口溢出、遮罩点击关闭与 80vh 滚动布局均未做像素级验证，仅有组件测试与 class 级证据。第 3–7 节的浏览器验收项保持未勾选，改由项目所有者按清单执行。
- **既有失败用例（与本次改动无关）**：`web/src/features/system-settings/hooks/__tests__/use-update-option.test.tsx`（11，`sonner` 的 `vi.mock` 未生效，已用 HEAD 版本复现）、`web/src/features/usage-logs/audit/__tests__/viewer.test.tsx`（与 `setup-guide.test.tsx` 同时运行时互相污染，单独运行分别通过/失败）、`web/src/features/models/metadata-editing.test.tsx`（超时）、`web/src/features/dashboard/components/overview/__tests__/setup-guide.test.tsx`（与 viewer 并发时 1 例失败，单独运行 6/6 通过）。
- **协议正文的事实核查**：第 3 节要求的数据收集/共享/留存核查（`model/log.go`、`service/error.go`、`service/task_polling.go`、Debug 日志、请求体临时文件、反向代理日志）本次未逐项复核，上线前仍需执行；内置正文已按“必要数据处理”如实说明，未声称完全零收集。
- **2FA 与过期版本**：二次验证流程内版本失效时会被拒绝，但错误映射为通用 `AUTH_INTERNAL_ERROR`，未返回 `legal_consent_outdated`；前端不会触发该路径（不会以过期版本发起流程）。
- **关于页支持联系方式**：`status.support_contact` 目前没有后台配置项，因此帮助区域始终隐藏该项；未虚构联系方式。
- **文档链接编辑入口重复**：新的编辑区域已加到「系统公告」页，但原计费/额度设置页的 `docs_link` 字段仍在（两处写同一配置键，无草稿冲突）；迁移入口与导航提示未实施。
- **国际化键**：7 个语言文件已按英文源字符串补齐本次新增键（`bun run i18n:sync` 报告 missing/extras/untranslated 均为 0）。后续若再修改语言文件，必须整文件读写并重新同步，避免只写英文而遗漏其他语言。
- **未推送**：提交与 Tag 尚未创建，推送前需确认远端与生产发布窗口。

### 9.5 最终差异与一致性检查

- `git status` / `git diff` 逐项复核：改动限定在五项需求涉及的 Go 与前端文件，未夹带无关重构；未修改任何受保护的项目归属标识（New API、QuantumNous、AGPL 链接、模块路径、许可证头）。
- 新增语言键全部通过整文件 JSON 读写写入，未使用文本插入；每个语言文件 6075 个键且 JSON 合法。
- 调试输出与临时文件已清理：`model/invite_reward.go`、`model/user.go`、`controller/*` 无 `DEBUG` 输出，无一次性脚本残留。
- `web/dist` 与 `web/src/routeTree.gen.ts` 已按需重建（Go 服务嵌入 dist，新路由需要重新生成的 route tree）。

## 10. 2026-09-17 本地验收与发布门槛

### 10.1 基线、范围与修复

本轮基线 `main@e5a3cf24aeaa4d00e0bfb3a031e3552e1c5bb183`，开始时工作区干净，领先 `fork/main` 4 个提交。Node `v24.12.0`、Bun `1.4.2`、Go `1.26.0 windows/amd64`；依赖与锁文件未更新，使用既有本地依赖。任务按 **P0、跨模块验收修复** 执行，重点为 PC Web、身份流程和奖励不重复入账。

验收发现并修复：

1. OAuth 登录 state 原来没有绑定发起浏览器。现在每个流程使用独立 HttpOnly、SameSite=Lax、10 分钟 Cookie，服务端只保存 HMAC；HTTPS 使用 `__Host-` 前缀。缺失、串用、重复 Cookie、过期和已消费流程在兑换上游 code 前被拒绝。多个流程可并行。
2. 过期 OAuth 协议原来在创建账号或迁移绑定之后才拒绝，现在在任何身份写入前复核。2FA/Passkey 二次验证的协议失效返回 `legal_consent_outdated` 和当前版本，不再返回通用内部错误。
3. 删除计费额度页的 `docs_link` 输入及旧默认值，改为导航到系统公告，避免两个表单各维护一份草稿。复用现有 Link、SettingsFormGridItem 和翻译键。
4. 关于页恢复 NewAPI、QuantumNous、One API、JustSong、AGPL 原始归属链接，覆盖空内容、Markdown、HTML、URL、加载和错误状态。现有 Footer 在非演示模式只展示 New API，不包含完整原始归属，因此使用 about 局部组合，不改全局页脚。隐私摘要去掉“不会向他人提供”的绝对表述，补明必要上游传输及其处理和留存政策；七语言同步。
5. 钱包无成熟奖励时保留禁用的提现按钮。响应丢失后的同金额重试复用尚未确认结果的幂等标识，同轮连续提交只发一个请求；明确成功或业务拒绝后开始下一笔。此标识保存在当前页面内存，重新载入前应先核对服务端奖励和余额状态。
6. 修复保存配置测试中 Sonner 包装覆盖 mock 的问题；限制 JSDOM 并发为 4、行为测试使用减少动态效果并等待实际异步结果。没有更改生产动效或跳过失败用例。

兼容与回滚：未改数据库结构或现有配置格式。部署前已发起且没有浏览器绑定的 OAuth 登录需重新发起，影响窗口最多为原流程 10 分钟；绑定和重新验证意图仍保留既有会话约束。提现、协议版本与之前五项功能的接口要求不变。生产未操作；上线备份与钱包回滚仍遵守第 7 节，不能直接退回不识别冻结账本的旧版本。

### 10.2 最终自动化结果

| 检查                                                                  | 当前结果                                                                 |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| `GOWORK=off go build ./...`、`go vet ./...`                           | 最终认证代码冻结后均通过                                                 |
| `GOWORK=off go test ./... -p 1 -count=1`                              | 44 个测试包通过，38 个无测试包                                           |
| `TestLoginConsentEnforcement`、`TestOAuthLoginBindsCallbackToBrowser` | SQLite/MySQL/PostgreSQL 各 2 顶层、22 子用例，通过且 0 SKIP              |
| `TestInviteReward*`、`TestInitializeInviteRewardLedger*`              | 8 顶层、21 子用例，三库通过且 0 SKIP                                     |
| 提现接口 × 主/独立日志库                                              | 三引擎合计 6/6 通过，重复请求不重复写日志                                |
| `bun run test`                                                        | 111 个文件、837 用例全部通过；本轮原始基线为 802 通过、28 失败           |
| `bun run typecheck`、`bun run build`                                  | 通过；重建 `web/dist` 后重新构建 Go 嵌入式服务                           |
| 改动文件 oxlint、oxfmt、`git diff --check`                            | 通过                                                                     |
| `bun run i18n:sync`                                                   | 七语言 missing / extras / untranslated 均为 0                            |
| `bun run lint`（全仓）                                                | 未通过：306 errors、77 warnings，均位于 134 个未修改文件；不冒充全仓通过 |

真实数据库版本：SQLite `3.50.4`、MySQL `8.0.46`、PostgreSQL `16.15`。全部使用新建隔离数据库，未清理或改写原有/生产数据库。

完整启动迁移使用各版本真实 `model.InitDB()` / `model.InitLogDB()` 建库，分别从空库、fork 实施前 `132c9a6ea`、上游 `v1.0.0-rc.35`、实时核实的最新发布 `v1.0.0-rc.37` 升级。四基线 × 三引擎 × 共享/独立日志库合计 **24/24 通过**。每组由独立进程重复初始化，第二次 DDL=0，表列索引摘要一致；钱包、历史余额、邀请码计数、配置、令牌、日志和唯一约束保持正确，历史结转只生成一笔。没有把当前模型构造旧用户当作真实旧版迁移证据。

SQLite 的独立日志库测试在主库初始化后切换隔离 `SQLitePath` 再执行 `InitLogDB`，验证两个真实物理文件；这不意味着常规启动环境变量支持两个 SQLite 路径。MySQL/PostgreSQL 使用各自独立日志库。未涉及最低版本特定 SQL，未另测 MySQL 5.7.8 / PostgreSQL 9.6。

证据保存在本机忽略目录 `.local-tests/next-acceptance/`：`backend-report.md`、`backend-20260917001013-auth-*.log`、`backend-20260917001128-go-*.log`、两份 `backend-upgrade-*-summary.log`、`frontend-final-test.log`、`frontend-final-build.log`。后端入口为 `backend-verify.ps1 -Mode all/core/auth`，迁移入口为 `backend-upgrade-matrix.ps1`；命令和版本详情在报告中。未修改 `relaykit/`。

### 10.3 桌面浏览器验收

Chromium `151.0.7922.34` + Playwright，隔离站点 `127.0.0.1:3017` 与最终构建 `127.0.0.1:3018`。测试账户、配置、账本和金额均为本地夹具。主要截图尺寸为 1440×900、1280×720，补充 900×720 窄窗口；深浅主题均检查。结果汇总 `browser-results.json` 含 39 项断言，截图位于 `screenshots/`，业务流程没有未处理的浏览器异常。

- 登录：默认未选、禁用门禁、勾选登录、登出后重置；通过界面启用 TOTP 后完成密码加验证码登录。未登录初始化的 `/api/user/auth/refresh` 401 为预期状态。
- 公告：实际新增、编辑、置顶、取消置顶、删除和保存；100 条时间轴全部可达、置顶优先；长文本和长链接正常换行，720px 宽、80vh 最大高度，仅正文滚动；右上关闭、关闭公告、今日关闭、Esc、遮罩、焦点圈定和动画结束后焦点恢复均通过。普通关闭跨路由保持，刷新重开；今日关闭刷新仍生效，手动查看始终可打开。日期逻辑另由既有跨日单元测试覆盖。
- 文档：界面保存后 Header 无需刷新即更新 href，刷新后保持，目标使用 `_blank` 和 `noopener noreferrer`；最终构建额度页只留导航且确实打开系统公告唯一输入。URL 安全与权限拒绝由接口和组件回归覆盖。
- 关于：默认介绍、长 Markdown/长 URL、隔离 HTML、URL iframe 兼容模式实际检查；HTML 注入脚本未执行，URL iframe 保留无 `allow-same-origin` 的 sandbox。最终构建归属链接、隐私摘要、深浅主题及窄窗口通过；错误和重试由真实网络边界的组件测试覆盖。
- 钱包：两笔独立奖励呈现冻结/可提现状态；到期刷新不会自动入账，手动提现只扣成熟奖励，另一笔继续冻结，刷新后明细正确；相同请求重放不重复记款。最终构建再注入“服务端已入账但浏览器响应连接中断”，用户重试复用原请求标识，钱包只增加一次；只有冻结/无成熟奖励时提现按钮可见且禁用。

最终可查看站点为 `http://127.0.0.1:3018/about`。该服务使用独立测试库，与生产无关；停止后可用 `.local-tests/next-acceptance/new-api-final.exe --port 3018` 启动，但必须同时保留隔离数据库路径和原会话密钥环境，不应默认连接其他库。

### 10.4 数据处理与认证事实核查

| 路径                 | 代码事实与证据                                                                                                                                                                                                      |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 普通文本、流式和图片 | 常规转发以内存处理；开启 `DEBUG=true` 会写完整请求、响应或 SSE。见 `relay/compatible_handler.go:141`、`relay/channel/openai/relay-openai.go:249`、`relay/helper/stream_scanner.go:252`、`relay/image_handler.go:79` |
| 异常正文             | `service/error.go:96` 可写上游响应片段；`common/str.go:18` 的 2048 字节截断不等于隐私脱敏                                                                                                                           |
| 异步任务             | `service/task_polling.go:544` 持久化上游响应，`:627` 仅裁剪特定 base64/video 字段；`model/task.go:70` 有私有轮询凭据/结果地址和响应数据，未找到统一任务自动 TTL                                                     |
| 请求体临时文件       | 磁盘缓存默认关闭，开启后默认 10MB 阈值写系统临时目录，0600 文件权限；`middleware/body_cleanup.go:11` 请求结束清理，启动清理超过 5 分钟残留，不等于统一保留 5 分钟                                                   |
| 账户、计费、IP       | `model/log.go:59` 保存账户、用量、计费和请求标识；调用日志 IP 可选，但 `model/audit_log.go:74`、`middleware/logger.go:43` 的认证/访问审计包含 IP、UA                                                                |
| 缓存与会话           | 会话保存刷新令牌摘要；流程消费后约 24 小时由清理任务移除，撤销会话默认保留 7 天，访问 JWT 15 分钟、登录会话 30 天；生产配置需另核实                                                                                 |
| 日志留存             | `service/system_task.go:77,168` 支持按时间手动清理数据库日志，`controller/performance.go:268` 支持按天/数量清理文件；未配置统一自动保留天数                                                                         |
| 必要共享             | 请求转发至所选模型上游；`service/channel.go:19` 的渠道异常原因还可通过 `service/user_notify.go` 发往管理员邮件/Webhook/Bark/Gotify，须核对通知服务与接收方                                                          |
| 反向代理与备份       | 仓库不能证明生产访问日志、正文缓存、轮转、访问权限或备份清理策略；本次未读取生产配置                                                                                                                                |

已在线核实 OWASP ASVS 稳定版为 **5.0.0**，参考第 8 节 Authentication、Session Management、CSRF、OAuth2 及 MFA Cheat Sheets。相关控制：V6.3.1、V6.3.8、V6.5.1、V7.2.4、V7.4.1、V10.1.2、V10.2.1、V14.1.1–2、V16.2.5。会话轮换/撤销、OTP、Origin 防护及 Redis IP/用户/邮箱限流专项 13 个顶层用例通过；日志角色隔离、错误敏感信息、任务裁剪、请求体生命周期回归通过。该结果不是整体 ASVS 合规认证。

### 10.5 仍需生产证据的事项

- [ ] 按实际启用项完成外部 OAuth、微信、Telegram、Passkey 真实回调和认证器操作。当前本地 Passkey 关闭，未为验收冒用外部身份或凭据；三库入口回归不等于外部提供方联调。
- [ ] 明确数据库调用日志、异步任务、审计与备份的实际保留期限、清理机制和访问权限，核对生产 `DEBUG`、请求体磁盘缓存、通知服务、反向代理及上游政策。代码清单已完成，运营“不售卖/不常态人工阅读”等事实仍需站点所有者确认。
- [ ] 核对生产 HTTPS、`SESSION_COOKIE_SECURE`、可信 Origin/代理配置，以及 ASVS V16.3.1 的失败认证审计缺口；`controller/user.go` 的既有策略只记录成功登录，未在本轮擅改全站审计策略。
- [ ] 全仓 lint 存量问题单独治理；本轮只保证修改文件无 lint error。
- [ ] 生产备份、维护窗口和真实库迁移须另行授权。本轮提交/Tag/推送只交付代码和验收记录，不代表已部署。

本轮最后成功检查：前端全量 837/837、typecheck、生产 build、后端全量/build/vet、三库认证/奖励/提现及 24 组迁移。临时依赖无变更，原有用户修改未回退，真实凭据未写入源码、测试或报告。验收证据和隔离数据暂留供复核。

## 11. 2026-09-17 关于页内容纠正检查点

本次为 **P2、关于页局部展示调整**，基线为 `main@33befb11d`。关于页以站点原有运营说明为重点：充值咨询、Codex / ChatGPT 远程接入协助、使用问题联系管理员，以及 QQ 群交流。管理员账号保留为 `hkcustom0928`，QQ 群保留为 `811481586`，不推断管理员账号所属平台。

- 首屏按三项服务和两项联系方式整理，复制交互复用现有 `CopyButton`；删除泛化的 API 接入介绍与三步流程。
- 未配置正文时显示上述运营说明；原有完整单行运营说明由整理后的版式展示一次。管理员新增的其他文本或 Markdown 继续渲染，HTML / URL 兼容模式、隐私摘要及原始项目归属保留。
- 七语言各增加 11 个键，没有改动其他翻译；临时 `web/scripts/add-missing-keys.mjs` 已清理。未改依赖、锁文件、接口或数据库。

已核对的检查结果：

| 检查 | 结果与证据 |
| --- | --- |
| 关于页测试 | `about-support-after.log` 记录 10/10 用例通过；顶部留白调整后于 08:29 再次运行同一测试文件，10/10 通过，包含原有运营正文不重复展示和两项复制交互 |
| `bun run typecheck` | 本轮重新执行通过，日志 `about-support-typecheck.log` |
| `bun run build` | 本轮生产构建通过，Rsbuild 2.1.6，日志 `about-support-build.log` |
| 改动文件 oxlint / oxfmt | 3 个 TSX 文件按 `.oxlintrc.json` 检查通过（退出码 0，无输出），全部 10 个改动源文件格式检查通过；格式日志 `about-support-format.log` |
| 七语言同步 | `_sync-report.json` 的 missing / extras / untranslated 均为 0；另逐个核对新增 11 个键在七语言均存在且非空 |
| `git diff --check` | 通过；没有把第 10 节记录的全仓 lint 存量问题标为已解决 |

日志继续保存在 `.local-tests/next-acceptance/`。当前最新前端预览为 `http://127.0.0.1:3019/about`，仅监听 `127.0.0.1:3019`，使用项目 `bun run preview --host 127.0.0.1 --port 3019 --strict-port` 前台启动，环境变量 `VITE_REACT_APP_SERVER_URL=http://127.0.0.1:3018` 代理至已有隔离后端。页面和 `/api/status`、`/api/about` 均返回 200。已有 `3018` 后端未停止、重启或改动，其内嵌页面仍为第 10 节构建。

首轮浏览器检查后补足首屏顶部留白，为固定导航预留空间；此后重新执行关于页测试、typecheck、生产构建及 TSX lint/format，均通过。

- [x] 最新 `3019` 构建完成桌面浏览器验收：1440×900 浅色、1280×720 深色、900×720 深色截图逐一检查，三项服务和联系方式均清晰可见，没有文字重叠或横向溢出，标题未被固定导航遮挡。
- [x] 实际点击两项复制按钮并读取剪贴板，分别得到 `hkcustom0928`、`811481586`；浏览器捕获的 error / warn 日志为空。验收后恢复浅色主题及默认视口，保留 `3019/about` 预览页。
- [x] 最终 diff、`git diff --check`、新增行凭据与调试输出检查通过，改动仅涉及本节记录的 11 个文件。

本轮截图位于 `.local-tests/next-acceptance/screenshots/about-support-{1440-light,1280-dark,900-dark}.jpg`。交付目标为 `main` 的已配置上游 `fork/main`（`https://github.com/lysimportant/forknewapi.git`）；本次局部前端修正按小改动提交，不新增 Tag。提交与远端一致性以最终 Git 核验结果为准。

本检查点不表示已部署生产；第 10.5 节的生产证据与发布门槛继续适用。

## 12. 2026-09-17 公告入口、协议弹窗与注册登录修复

本轮为 **P1、跨前后端行为修复**，基线为干净的 `main@997ac346648856ca87a2ca55ca4d2b187abe19f9`，上游为 `fork/main`。Node `24.12.0`、本地 Bun `1.4.2`、Go `1.26.0`；依赖和锁文件无变更。目标是公共页面自动公告、协议原地阅读、注册无需同意而登录必须主动同意。PC Web 为验收对象，不扩展生产部署或其他业务。

本节覆盖第 9、10 节中的历史“登录按钮禁用”“注册也必须同意”“仅控制台自动弹公告”行为，第 3、4 节的交互规则已同步更新。

### 12.1 修复内容与兼容边界

- 公共 `PublicHeader` 原来调用通知 hook 却没有挂载 `AnnouncementTimelineDialog`，且自动判定遗漏旧 `/api/notice` 正文。现在公共页和控制台均复用原公告弹窗，展示旧正文与完整时间轴；通知入口可手动查看全部公告。
- 普通关闭只影响本次页面会话，站内路由不重复弹出，刷新再弹；“今日关闭”当天跨刷新有效，手动查看不受限制。时间轴展示开关只控制时间轴，旧 Notice 独立展示；无内容不弹空窗。
- `LegalDocumentDialog` 组合项目业务 `Dialog`、`ErrorState` 和 `RichContent`，按需读取原有三种协议 API，复用滚动、键盘、焦点恢复及安全正文渲染。登录、注册、关于页和全站页脚入口均在 Dialog 阅读，不改变 URL；打开/关闭不自动同意。错误、空正文可重试，独立协议页面保留给历史链接。
- 登录按钮可点击，但统一守卫先检查协议，再进入字段校验或认证。未勾选时点击/回车显示“请先阅读并同意协议后再登录。”；状态加载失败时提示重新加载。OAuth、微信、Passkey 保留相同守卫，服务端登录版本校验及二次验证流程不变。
- `POST /api/user/register` 不再要求 `consent` / `consent_version`，只创建账号，不颁发登录令牌或 Cookie。前端不再提交这两个字段，注册成功后返回默认未勾选的登录页。旧客户端附带字段仍可兼容；新客户端应与本轮后端同时交付，否则旧后端仍会拒绝缺少注册协议字段的请求。
- 注册页的第三方入口会建立登录会话，因此先导回登录页阅读和确认协议。注册页补充文档只供阅读，不再声称“创建账号即同意”。
- 七语言各增加一条登录提醒，通过临时 `add-missing-keys.mjs` 及 `i18n:sync` 写入；临时脚本已删除。复用现有 UI 封装，没有新增通用弹窗、状态管理或渲染框架。

本轮不改变数据结构、迁移、密码校验、验证码、MFA、会话轮换、Origin 防护或限流。无需迁移用户数据，未修改真实库；如需回滚本次代码，前后端一起回退到基线提交即可恢复旧注册协议门禁，不需要回滚数据库。本说明不授权生产回退或跳过第 7 节的钱包发布要求。

### 12.2 自动化验证

| 检查 | 本轮结果 |
| --- | --- |
| `bun run test src/features/auth src/features/announcements src/hooks/__tests__/use-notifications.test.tsx src/components/layout/components/__tests__ src/features/about` | 最终 18 文件、128/128 通过，日志 `entry-consent-final-tests.log` |
| `bun run typecheck`、`bun run build` | 最终均通过，日志 `entry-consent-final-typecheck.log`、`entry-consent-final-build.log` |
| `GOWORK=off go vet ./controller/`、`go build ./...` | 通过；最终另用本地 loopback overlay 构建最新前端嵌入服务，通过 |
| `backend-verify.ps1 -Mode auth` | 真实 SQLite `3.50.4`、MySQL `8.0.46`、PostgreSQL `16.15` 全部通过，无 SKIP |
| 修改文件 oxlint / oxfmt / `git diff --check` | 无 error，格式与 diff 通过；Footer 的既有 `dangerouslySetInnerHTML` 保留一条 warning，不宣称全仓 lint 通过 |
| 七语言同步 | `_reports/_sync-report.json` 中 missing / extras / untranslated 全部为 0；locale diff 每种语言仅新增一个键 |

前端先复现公共公告缺失和页面协议跳转，再修复并验证。整合过程中 About 测试原来将正文桩响应返回给公告请求，新增公共弹窗后暴露夹具串用；已显式设定该夹具没有公告，最终相关集合完整通过。后端回归确认无协议字段可以注册但没有活动会话、Cookie 或 access token，同一新账号未同意时登录仍拒绝。原有协议过期、OAuth 浏览器绑定、2FA/Passkey 版本校验继续通过。

三库日志为 `.local-tests/next-acceptance/backend-20260917111619-auth-{sqlite,mysql,postgres}.log`，执行 `GOWORK=off go test ./controller/ -run 'TestLoginConsentEnforcement|TestOAuthLoginBindsCallbackToBrowser' -count=1 -v`，由脚本注入隔离数据库配置。没有把单库或 mock 当作三库证据；本轮不涉及引擎最低版本特定行为。

认证修改前重新读取 OWASP Authentication 与 Session Management Cheat Sheets，适用控制为服务端强制登录校验、注册不建立会话、备用入口不得绕过、不得在日志写入凭据。ASVS 5.0.0 与既有认证、生产审计限制继续见 10.4–10.5；本次验证不代表全站 ASVS 合规认证。

### 12.3 桌面浏览器验收与运行检查点

最新隔离预览：`http://127.0.0.1:3020/sign-in`（关于页为 `/about`），仅监听 `127.0.0.1:3020`。使用本轮前端生产构建及最新 Go 二进制，独立 SQLite `entry-consent-20260917.db`，公告、协议与注册账号均为本地测试夹具，未接入生产。旧 3018/3019 服务保留，最新验收应使用 3020。

- [x] 首次进入 about 自动弹出只有旧 Notice 正文的公告；关闭后切换首页不重弹，刷新首页重新弹出。
- [x] 选择今日关闭后刷新不弹，通知“查看全部公告”仍可手动打开。
- [x] 登录初始 checkbox 未勾选，空表单及已填写正确测试账号两种情况下，点击登录均显示中文提醒并保留当前页。
- [x] 登录协议长正文在 1280×720 窗口呈现 720×576 Dialog，仅正文滚动，可读到最后一节；Esc 关闭后焦点回到入口，URL 与未勾选状态不变。
- [x] 实际创建隔离测试账号，注册页没有协议 checkbox；成功返回登录页默认未勾选，同意后成功进入控制台，登出后重新未勾选。
- [x] 最终构建的关于页“阅读协议全文”、页脚“用户协议”“隐私政策”均原地显示对应正文，URL 保持 `/about`。
- [x] 1440×900 和 1280×720 画面检查通过；最终浏览器捕获的 error/warn 为空，恢复默认视口并保留最新登录页。

证据保存在 `.local-tests/next-acceptance/`：`entry-browser-results.json`、`screenshots/entry-{notice-1440,login-message-1440,agreement-1280,register-1280,footer-dialog-1280}.jpg`。隔离服务日志为 `entry-server-final.log`；二进制为 `new-api-entry-final.exe`，仅本地 overlay `entry-loopback-overlay.json` 将监听地址限制到 loopback，仓库 `main.go` 未改。启动时必须指定本节隔离 SQLite 路径、清空其他 SQL/Redis DSN，并在进程环境生成独立会话/加密密钥；重新生成密钥会使本地测试会话失效。不得不带隔离环境直接启动到其他库。

最后成功阶段：128/128、typecheck、生产构建、三库认证、Go build/vet、最新构建浏览器烟测和修改文件检查。交付至 `fork/main`，按跨模块接口行为变更创建中文 annotated Tag；提交、Tag 和远端一致性以最终 Git 核验为准。第 10.5 节生产待办继续保留，另需注意管理员配置的外部协议 URL 若禁止 iframe 嵌入，不能在 Dialog 内强制加载，建议配置正文。

## 13. 2026-09-17 公告保存与内容来源修复验收

本轮为 **P1、前后端配置保存修复**，从 `main@9ffa60abd` 的未提交改动继续，上游为 `fork/main`。Node `24.12.0`、本地 Bun `1.4.2`、Go `1.26.0`，依赖已存在，未安装或升级。验收目标为添加、编辑、置顶、单条及批量删除确认后立即持久化，失败保留输入和旧值，时间轴 Dialog 只显示系统公告列表，PC Web 刷新及重新启动后结果一致。

不改变数据库结构、配置格式、认证或计费规则，不操作生产数据。数据库验证使用独立 SQLite/MySQL/PostgreSQL；失败事务不得留下空配置行或更新内存。回滚仅需回退本轮代码，无数据迁移。本轮覆盖第 12 节的旧 Notice 混入时间轴行为，旧 Notice 仍可通过通知面板阅读。

### 13.1 原任务与 agent 分工核对

原任务 `01a0aa79-8dbb-70d2-848d-70b0ae3afd1c` 的最后公告反馈轮中断，不能以此前第 12 节的完成记录替代本次验收。

| 责任方 | 已核实工作 | 状态与缺口 |
| --- | --- | --- |
| 原主 agent | 确认新增只更新页面草稿；从公共及控制台 Header、时间轴 Dialog、自动弹出钩子移除旧 Notice，调整相关测试 | 未完成整体验证、检查点、提交和推送 |
| 原 `notice_entry_fix` sub agent (`01a0ad55-5dd1-7250-b5bd-af1b4725dab2`) | 先只读定位双重保存与旧查询竞争；随后负责公告管理及其测试，改为立即保存、失败保留、保存中禁止冲突操作，删除复用 ConfirmDialog | 早期 5 用例和 typecheck 曾通过；最后扩至 8 用例时仍有 2 失败，不能沿用旧通过结论 |
| 原 `auth_consent_flow` sub agent (`01a0ad55-fb04-70e1-bd32-4c726464a8f8`) | 本反馈轮转为负责 `model/option.go` 和集中测试，事务检查数据库错误后才更新内存 | SQLite 通过；MySQL/PostgreSQL 因未配置 DSN 被 SKIP |
| 本轮主 agent | 读取上述主/子任务工具记录、实际差异与检查点；修正失败测试，补 AbortSignal 和设置查询去重边界、取消提示处理；完成前端全量、浏览器真实保存/刷新/重启验证与 Git 交付 | 验收完成；提交、Tag 和推送以最终 Git 核验为准 |
| 本轮 `option_persistence` sub agent | 修正 MySQL 保留字测试查询，补成功更新/清空/重复保存与连接失败测试，执行真实三库、Go 全量测试及 build/vet | 已交付，主 agent 已核对源码哈希与日志 |
| 本轮 `announcement_review` sub agent | 只读复现取消查询后再次回读复用旧 HTTP Promise 的竞态，提出设置查询传递 signal 并交由 React Query 去重；最终复审无阻塞发现 | 已交付，没有修改文件 |

### 13.2 验收结果与交付边界

- [x] 前端恢复基线：5 文件，25 通过、2 失败，并有 JSDOM `getAnimations` 缺失异常；证据 `announcement-recovery-baseline.log`。失败分别涉及 Sonner 包装后监听失效和同一错误断言等待超时。
- [x] 三库保存/回滚：SQLite `3.50.4`、MySQL `8.0.46`、PostgreSQL `16.15` 无 SKIP；Go build/vet 与配置相关回归通过。证据前缀 `announcement-backend-20260917150129714-`。
- [x] 最终后端全量 `GOWORK=off go test ./... -p 1 -count=1` 通过，耗时 294.9 秒；日志 `announcement-backend-20260917160712360-go-test-full.log`，独立三库配置由验收脚本注入。controller 完整包通过，未把该结果表述为所有测试都覆盖三库。
- [x] 最终前端全量 113 文件、860/860 通过，日志 `announcement-frontend-final.log`；公告管理 9/9，typecheck、修改文件 lint/format 和生产构建通过。公共通知测试的 `getAnimations` 适配限定该文件并恢复原属性。第一次全量检查 859/860，API Key 抽屉出现一处即时断言失败；收窄测试环境后专项 5/5 与最终全量均通过，没有改动 API Key 功能代码。
- [x] 最新隔离构建 `http://127.0.0.1:3021/about` 的 10 项浏览器检查通过：新增、编辑、置顶/取消置顶、单条/批量删除立即保存；重新加载与进程重启保持公告；写入失败保留输入与数据库旧值并可重试；旧 Notice 只在通知面板；普通关闭刷新重开、今日关闭抑制自动但允许手动查看。浏览器未处理异常及 error 控制台为空（匿名初始化 401 属预期请求，单独排除）。
- [x] 最终证据、完整 diff、修改文件格式与敏感信息检查通过。交付目标为 `fork/main`（`https://github.com/lysimportant/forknewapi.git`），按共享数据库写入行为变更创建中文 annotated Tag `v1.0.0-rc.35.custom.12`；提交和远端一致性由最终 Git 输出核验。本轮不授权生产部署。

范围外记录：`TestUpdateOptionAliasBillingExprUsesPluginSchema` 单独运行因夹具未初始化 `commonKeyCol` 生成缺列查询；用 HEAD 原始 `option.go` overlay 同样复现，日志 `announcement-backend-20260917153727203-baseline-controller.log`。完整 controller 包最终通过，但独立运行夹具问题仍应单独治理，未修改计费模块。全仓 lint 存量问题及第 10.5 节生产待办继续保留。

可复核命令：`bun run test`、`bun run typecheck`、`bun run build`；触及 TS/TSX 文件使用 `node node_modules/oxlint/bin/oxlint -c .oxlintrc.json <文件>` 和 `node node_modules/oxfmt/bin/oxfmt --check <文件>`。后端执行 `GOWORK=off go build ./...`、`go vet ./...` 及上述全量测试；三库专项为 `go test ./model/ -run '^TestUpdateOptionPersistsAndPropagatesDatabaseErrors$' -count=1 -v`，必须提供独立 `TEST_MYSQL_DSN`、`TEST_POSTGRES_DSN`。本地脚本 `announcement-backend-verify.ps1 -Mode all` 与 `-Mode full` 保存对应脱敏证据。

### 13.3 隔离运行与界面证据

浏览器脚本为 `.local-tests/next-acceptance/announcement-browser.cjs`，结果为 `announcement-browser-results.json`，服务日志为 `announcement-server.log`。后端二进制 `new-api-announcement.exe` 嵌入最新前端，复用仅本地 `entry-loopback-overlay.json` 绑定 `127.0.0.1:3021`；仓库监听代码未修改。测试专用数据库为 `announcement-site-1789632159953/site.db`，与此前 3018/3019/3020 和生产库隔离，测试账号及会话密钥运行时生成，没有写入代码或报告。

1440×900 的管理页、编辑失败状态、公告 Dialog 与 1280×720 的 Dialog 截图均已逐张检查，正文与按钮无重叠或横向溢出，弹窗保持原有布局和关闭入口。截图为 `screenshots/announcement-{settings-1440,edit-failure-1440,timeline-1440,timeline-1280}.png`。组件继续复用 `Dialog`、`ConfirmDialog`、`StaticDataTable`、`StaticRowActions`、`DateTimePicker`，没有新增通用 UI。49 个触及文件中的翻译键在七语言均存在且非空，未修改 locale、依赖或锁文件。

另补中文默认画面 `screenshots/announcement-timeline-zh-1440.png`：系统公告、关闭公告、今日关闭与置顶标记均正常，实测 Dialog 宽 720px、高 239.75px。源代码及验收日志未检出凭据或新增调试输出；本地生成账号只用于隔离验收，不用于生产。

## 14. 2026-09-17 rc.37 定制兼容升级

固定合入官方 `v1.0.0-rc.37`，基于 fork `6e44f3d99` / `v1.0.0-rc.35.custom.12`，新版本为 `v1.0.0-rc.37.custom.1`。在独立工作区解决 46 个冲突，保留公告、登录协议及注册流程、QQ 邮箱提示、关于/文档、48 小时奖励、兑换码、API 地址、耗时颜色、Responses 和现行视频定制；部署配置不变。

完整能力清单、命令、产物哈希、回滚边界及主/子 agent 实际分工见 [升级报告](verification/rc37/upgrade.md)。主 agent 处理认证、语言、整合验收与 Git 交付；`announcement_review` 处理公告/公共前端、真实浏览器和粘贴修复；`option_persistence` 处理模型迁移、奖励账本及真实三库；`release_baseline` 处理计费、插件、定价界面及 Responses 缓存模态兼容。

- [x] 最终前端 154 文件/1655 测试及新增粘贴 4 项、typecheck、生产构建通过。
- [x] 后端 44 个测试包、build/vet、relaykit 独立 build/test 通过。
- [x] 三库 18 个迁移组合、36 次启动、六组提现、定价与 Passkey 专项通过；第二次启动无新增 DDL。
- [x] 同一最终 Windows 产物的公告 10 项和核心页面 14 项浏览器通过；原生 Ctrl+V 首行兑换、协议 Dialog、注册无会话、冻结奖励均实测。
- [x] 最终 Docker 产物 39 项隔离 HTTP 验收通过，包含 Responses 工具往返和视频结算退款。
- [x] 手工改动格式、lint、完整差异及凭据特征检查通过。全仓 lint 192 个既有 error 无新增；外部商店 403 单列限制。

验收还修复了冻结奖励重启重复结转、受邀赠送误入提现、配置迁移失败继续启动，以及新版缓存模态在 Responses 转换中丢失；未自动清理既有异常账本。新预览为 `http://127.0.0.1:3022/about`，原 3021 仍是旧版。所有数据库和付费上游请求均隔离，生产未部署。

交付目标为 `fork/main`（`https://github.com/lysimportant/forknewapi.git`）及中文 annotated Tag `v1.0.0-rc.37.custom.1`；提交和远端一致性以本次最终 Git 核验为准。第 10.5 节的生产备份、外部身份联调、日志保留期限及审计门槛继续保留。

## 15. 2026-09-18 钱包支付方式与充值返佣

本轮 P1，基线为干净的 `main@e47a383a8`，无依赖升级。钱包兑换码店铺新增支付宝 `https://catfk.com/shop/68AEJFZJ`，微信仍默认，Tab 同步切换内嵌店铺与新窗口入口。复用项目 Tabs、Button 和设置表单组件，七语言文案同步。

用户确认按成功充值的实际到账余额返还 10% 给直接邀请人，包含兑换码；每笔固定冻结 48 小时，仍手动提取至站内余额。取消邀请人固定注册奖励及其管理端输入，保留受邀人注册赠送、历史奖励、旧配置兼容读写；旧配置不能恢复固定注册奖励或取消新返佣冻结。不对历史充值追溯补发。额度以整数最小单位向下取整。

在线支付、管理员补单、兑换码均在原有事务内完成到账和返佣，奖励按订单/兑换码 ID 去重；重复回调不加款，额度超限整体回滚。普通/OAuth 创建时保存邀请人，邀请人数不随充值次数增加。奖励汇总统计完整账本，最近 50 笔仅限制明细；注册计数的零额度记录不展示为奖励。注册收尾与首次充值统一按受邀人、邀请人顺序加锁。

验收进展、具体命令和回退边界见 [任务检查点](verification/wallet-referral-checkpoint.md)、[数据库记录](verification/wallet-referral-database.md)、[浏览器记录](verification/wallet-referral-browser.md)。主 agent 负责账务代码、集成与 Git；wallet_ui 负责前端及检查，db_verification 负责三库与只读账务复核，browser_smoke 负责隔离真实浏览器。

范围外继续保留：全仓既有 lint 问题、第三方微信店铺嵌入 403、真实外部支付、生产部署及第 10.5 节待办。本轮未修改 schema 或生产数据；交付目标为 `fork/main` 和中文 annotated Tag `v1.0.0-rc.37.custom.2`。

最终本次专项 29 个前端测试、三库 121 条 PASS/六组提现/24 次启动、13 项浏览器通过；typecheck、构建及修改文件检查通过。全量前端 1662/1663，仅未改渠道权限场景超时（原提交可复现，当前定向 62/62 通过）；Go 全量 44 包通过，后续 controller 重跑有一次未改账号删除并发 SQLite 锁冲突，定向复核通过。这两项不稳定测试和全仓 192 个既有 lint error 继续单独跟踪，详情见检查点。

## 16. 2026-09-18 使用日志模型差异

本轮 P1，基线为干净的 `main@870b16c63`。用户确认同时展示请求与调用模型差异、调用与上游响应模型不一致。前端复用已有模型徽标与详情组件，直接显示映射模型和响应不一致提示，支持完整模型名复制，七语言文案同步；后端在请求最终参数确定后保存调用模型，在响应转换前记录上游声明，写入现有日志 Other JSON。

不把上游自报名称视为底层模型身份验证，不推测历史日志或缺失响应模型；保留现有计费、转发、数据库结构和权限。验收、接口附加字段、恢复步骤及覆盖范围见 [检查点](verification/usage-log-model-mapping.md)。前端 225 项日志测试、类型检查、修改文件 lint、生产构建与四组浏览器样例通过；后端六个相关包、终态补充回归、Go 构建与 vet 通过。交付为 `fork/main` 与 `v1.0.0-rc.37.custom.3`；全仓 192 个既有 lint error 继续单独跟踪，未部署生产。
