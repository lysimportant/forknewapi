# 下一阶段功能计划

更新时间：2026-09-16。状态：规划完成，功能尚未实施。

本次交付为根目录 `next.md`，对应登录协议、公告弹窗、关于页面、文档链接、推荐奖励提现五项需求。以下区分已核实的现状、计划行为和实施前需要确定的业务规则，不能将计划项视为已上线功能。

## 1. 范围、基线与优先级

### 初次规划基线

| 项目       | 已核实状态                                                                                                                        |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 仓库与分支 | `D:\newapi`，`main`，跟踪 `fork/main`；开始时工作区干净                                                                           |
| 基线提交   | `a5fb489a1fe4ae80f8266926fbc65977f6101120`，`feat(redemption): 多选工具栏增加批量删除`                                            |
| 交付远端   | `fork` → `https://github.com/lysimportant/forknewapi.git`；`origin` 是上游仓库                                                    |
| 运行时     | Node `v24.12.0`、Go `go1.26.0 windows/amd64`、仓库本地 Bun `1.4.2`；`go.mod` 要求 Go `1.25.1`                                     |
| 前端依赖   | 已存在 `web/node_modules`、`web/dist` 和 `web/bun.lock`；本次不安装或升级依赖                                                     |
| Bun 入口   | 当前 PATH 中没有 `bun`；已验证 `.local-tests/responses-docker/tools/node_modules/bun/bin/bun.exe` 可用                            |
| 测试基线   | 本次为文档任务，未执行应用测试或三数据库验证。`verification/rc35/upgrade-rc35-checkpoint.md` 是历史记录，不代表当前提交已重新验证 |
| 任务记录   | 根目录未发现现有 `TODO.md` 或 `next.md`；后续以本文件的阶段清单继续执行                                                           |

本次文档任务为 **P2、小范围文档变更**。后续实现以 PC Web 为主要验收对象，沿用现有 React、Base UI、Tailwind、Go/Gin 和配置体系，保留当前定制行为、New API 与 QuantumNous 的项目归属信息。

| 后续工作                 | 优先级 | 完成标准                                                               |
| ------------------------ | ------ | ---------------------------------------------------------------------- |
| 登录协议及数据处理口径   | P0     | 登录前主动勾选，所有启用的登录入口遵守同一规则，声明与实际留存行为一致 |
| 推荐奖励逐笔 48 小时限制 | P0     | 服务端逐笔判断，主动操作才到账，并发、重试和历史迁移不重复计款         |
| 公告管理及时间轴弹窗     | P1     | 可增改删、置顶，三个关闭入口行为明确，“今日关闭”跨刷新生效             |
| 文档链接配置入口         | P1     | 系统公告页可设置 URL，保存后 Header 使用新地址                         |
| 关于页面                 | P2     | 有完整介绍与清晰版式，保留已有自定义内容模式                           |

本轮不实施上述功能，不操作生产环境、不迁移真实数据库、不引入外部现金打款系统。若后续确认需要现金提现，先补全该业务方案再实施。其他优化记录为后续项，不夹带无关重构。

## 2. 已核实的现有能力

| 需求     | 当前实现与缺口                                                                                                                                    | 主要入口                                                                                                                                                                         |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 登录协议 | 已有 `LegalConsent` 和登录按钮禁用逻辑，但依赖管理员填写用户协议或隐私政策；未配置时不展示。密码登录请求目前没有同意标记或协议版本                | `web/src/features/auth/components/legal-consent.tsx`、`web/src/features/auth/sign-in/components/user-auth-form.tsx`、`web/src/features/auth/api.ts`、`controller/user.go`        |
| 系统公告 | 有单文本 `Notice`，另有支持增改删的 `console_setting.announcements` 列表；列表已有发布时间和类型，缺少置顶、自动弹窗及时间轴                      | `web/src/features/system-settings/maintenance/notice-section.tsx`、`web/src/features/system-settings/content/announcements-section.tsx`、`setting/console_setting/validation.go` |
| 今日关闭 | 通知 store 已有 `closedUntilDate` 和 `isNoticeClosed()`，尚未接入目标弹窗                                                                         | `web/src/stores/notification-store.ts`                                                                                                                                           |
| 关于页面 | `About` 配置已经支持 Markdown、隔离 HTML、URL iframe；未配置时显示空状态。需要改进单行内容的版式并补充站点介绍                                    | `web/src/features/about/index.tsx`、`web/src/features/system-settings/general/system-info-section.tsx`                                                                           |
| 文档链接 | 已有 `general_setting.docs_link`，当前编辑入口在计费/额度设置；Header 已读取 `/api/status` 的 `docs_link`，保存后的状态刷新白名单尚未包含该配置键 | `web/src/hooks/use-top-nav-links.ts`、`web/src/features/system-settings/general/quota-settings-section.tsx`、`web/src/features/system-settings/hooks/use-update-option.ts`       |
| 推荐奖励 | 邀请注册后累计 `aff_quota`，用户通过“转入余额”手动转到站内钱包；没有逐笔奖励时间、48 小时冻结或现金提现系统                                       | `web/src/features/wallet/components/affiliate-rewards-card.tsx`、`web/src/features/wallet/components/dialogs/transfer-dialog.tsx`、`controller/user.go`、`model/user.go`         |

以上来自代码检查，未读取线上配置，因此不推断当前站点的协议正文、公告内容或实际日志保留设置。

## 3. 登录按钮下方的 API 服务及隐私协议

### 页面与交互

- 在主登录按钮正下方展示未勾选的 checkbox，文案为“我已阅读并同意《API 服务、隐私与使用责任协议》，并承诺依法使用本平台 API 服务。”；点击协议名称可查看完整内容，点击链接不能意外改变勾选状态。
- 勾选区域的补充说明为：“本站仅提供大模型 API 接入服务，不出售用户数据或个人信息，不对您的具体用途进行常态化人工监测。您应依法使用服务，并对自身违法使用行为依法承担相应责任。”完整的数据处理范围及必要处理情形见协议正文。
- 默认未勾选，未同意时登录按钮禁用；键盘回车、表单提交和备用登录入口也必须受同一状态控制。
- 扩展现有 `LegalConsent`，与已有用户协议/隐私政策合并展示，避免两组重复 checkbox。旧法律文本保留；通过现有站点系统信息中的协议配置补充内容。
- 即使原法律配置为空，本站 API 服务说明仍提供默认内容和强制勾选；状态加载中或失败时不能短暂放开登录。密码登录被关闭时，协议仍在备用登录区域可见。
- 每次新的登录流程重新同意；同一次 OAuth 跳转、Passkey 或二次验证过程保留已确认状态。退出登录后再次登录不能自动沿用上次勾选。
- 共享组件也被注册页使用，实施时明确登录场景的配置边界，并验证注册页原有协议行为。

### 登录勾选协议正文

协议名称：**《API 服务、隐私与使用责任协议》**。以下正文供登录页面的协议弹窗或现有协议页面使用；上线前按下节核对实际数据处理行为。

> **一、服务范围**
>
> 本站仅提供大模型 API 接入、调用及请求转发等技术服务。您自行决定服务的具体用途、提交的内容以及生成结果的使用方式，并应对提交、发布或使用的内容进行必要核实。
>
> **二、用户数据与隐私保护**
>
> 本站不出售、出租您的个人信息、账户资料、提问内容或模型回复，不非法收集、使用、披露或向他人提供上述数据，也不将您的提问、模型回复及其中的隐私信息用于模型训练、用户画像或营销。
>
> **三、必要的数据处理**
>
> 本站仅在完成 API 请求及依法履责所需范围内处理您的提问、模型回复和请求数据，不进行与提供服务无关的额外收集。为提供账户认证、用量计费和安全维护，本站会在必要范围内处理、保留账户资料、调用用量、计费记录及安全日志；处理和保留限于实现相关目的所需的范围与期限，法律另有要求的除外。本站依法采取数据保护措施，请勿提交完成请求所不需要的敏感信息。
>
> **四、使用与监测边界**
>
> 本站不主动追踪您的具体使用场景，不对您的提问和模型回复进行常态化人工阅读或监测。为履行适用法律法规、保障服务安全、处置滥用行为或响应有权机关的合法要求，本站可在必要范围内开展安全检测、风险处置及依法配合。上述服务边界不表示本站允许或认可任何违法用途。
>
> **五、合法使用与用户责任**
>
> 您应遵守适用法律法规及相关上游服务规则，确保对提交的数据具有合法使用权限，不得利用本站从事违法活动或侵害他人的合法权益。因您违法使用服务或侵害他人权益而产生的相应法律责任，由您依法承担。本站提供 API 接口不构成对您的具体用途或生成内容的认可、授权或合法性保证。
>
> **六、上游服务与必要处置**
>
> 为完成请求，相关内容会被传输至所调用的上游模型服务，其数据处理和留存规则适用该服务的政策；本站的数据承诺不替代上游服务的承诺。发现违法违规或危及服务安全的行为时，本站可依法依约采取限制调用、暂停或终止相关服务等必要措施。
>
> **七、责任边界与同意**
>
> 本协议关于用户责任的约定，不免除或限制本站依法应承担的个人信息保护、数据安全及其他法定义务和责任。勾选同意表示您已阅读并理解本协议，并承诺依法使用本站服务；如不同意，请勿继续登录和使用。

### 发布前的事实核查

这份协议已补齐“不售卖用户数据”“仅提供 API 服务”“不进行常态化人工监测”和“用户依法承担违法使用责任”的正文。发布前仍须核对数据收集目的、使用及共享情况、留存期限、用途追踪和人工监测方式；账户资料和计费记录不能被错误表述为不存在，上游服务的数据规则也不能由本站代为承诺。

“不监测”在此限定为不主动追踪具体用途、不常态化人工阅读内容；必要的安全检测、计费记录及依法处理须如实说明。“违法使用责任由用户依法承担”针对用户自身行为，不写成“任何责任均由用户承担”或“平台在任何情况下免责”。

实施前须核查普通文本、流式请求、图片/文件、异步视频任务以及异常路径。当前 `model/log.go` 有账号、用量及可选 IP 等字段；`service/error.go` 的异常日志可能包含上游响应片段；`service/task_polling.go` 会保存处理后的任务响应。还需核查 Debug 日志、请求体临时文件、数据库、缓存和反向代理日志。

如发现与协议冲突的额外收集、使用或共享，应先修正对应路径并验证，再发布协议。必要账户/计费记录的字段及保留期限、异步任务处理期是否需要暂存，须在协议定稿时明确；若发现需要保留请求正文的情形，应如实说明用途、范围和期限，不直接声称“完全零收集”或“绝不留存”。本次文案补充不授权删除现存数据或关闭必要安全措施，涉及清理需另行确认。

### 服务端与兼容性

- 登录请求增加同意标记及协议版本，服务端校验当前版本；在已有登录流程中绑定确认状态，最终建立会话前复核。拒绝缺失、未同意、过期版本以及失效/串用的流程。
- 覆盖密码、已启用的 OAuth、微信、Telegram、Passkey 和后续 2FA 流程；OAuth 在发起流程时绑定同意状态，保留第三方回调协议、state 和现有校验。
- 不以浏览器 localStorage 中的布尔值作为服务端同意凭据，不把协议确认当作身份认证，也不降低验证码、限流、MFA、会话轮换及 CSRF 防护。
- 若记录同意凭证，只保留必要的协议版本、时间和关联标识，不记录密码、验证码、令牌或请求正文。
- 强制服务端同意校验会影响直接调用登录接口的旧客户端，实施前列出调用方并同步升级；不能为了兼容缺少字段的请求而保留绕过入口。API Key 的模型调用不应被新增为交互式登录流程。

### 验收

- [ ] 默认未勾选；勾选后才能登录；取消勾选立即禁用；回车不能绕过。
- [ ] 协议能阅读，键盘可操作，加载/失败状态明确，注册页无回归。
- [ ] 所有启用的登录方式及二次验证均通过；直接请求、过期版本、回调串用和重放被拒绝。
- [ ] 会话轮换、验证码、限流和敏感日志控制通过针对性回归；数据使用、共享和留存核查有可复核结果。
- [ ] 登录勾选摘要、完整协议和关于页统一说明不售卖数据、服务与监测边界及用户使用责任；必要数据处理和平台法定义务没有被摘要省略成绝对免责。

## 4. 公告管理、时间轴弹窗与置顶

### 管理入口与数据

将管理入口集中到 **系统设置 → 站点 → 系统公告**（现有 `/system-settings/site/notice`），页面包含“公告列表”和“文档链接”两个区域。

- 复用已有 `AnnouncementsSection`，提供新增、编辑、删除、发布时间、类型、正文、额外说明、置顶/取消置顶及展示开关。
- 继续使用 `console_setting.announcements` 和 `console_setting.announcements_enabled`，不建立第三套公告数据。
- 保留旧字段 `id`、`content`、`publishDate`、`type`、`extra`，仅补充必要的 `pinned`。旧数据缺少 `pinned` 时按 `false` 读取；前后端均校验类型。
- 默认支持多条置顶。排序为“置顶优先 → 各组发布时间倒序 → 时间相同时保持稳定顺序”；弹窗、通知列表和仪表盘保持一致。
- 保留现有限制：最多 100 条，正文 500 字，额外说明 100 字，时间使用 RFC3339。若以后确需长公告再单独调整限制，不在本轮隐式放宽。
- 沿用手动保存即发布，发布时间用于展示，不额外增加定时发布、附件或草稿审批系统。
- 原内容设置页保留导航到统一编辑入口。旧 `Notice` 文本保留在兼容区域，继续维持已有读取契约；如管理员选择迁入列表，必须保留原文并避免重复展示，不自动覆盖或删除。
- 管理权限沿用 `/api/option/` 的 `RootAuth`；公开读取接口仅返回可公开的公告数据。保存失败保留草稿，保存成功刷新公告和 status 缓存。

### 弹窗布局

复用 `web/src/components/dialog.tsx` 的关闭按钮、内容滚动和 footer 插槽，组合为公告业务组件。

```text
┌──────────────────────────────────────────────┐
│ 系统公告                                  × │
│                                              │
│ ● 置顶 · 2026-09-16 10:00                     │
│ │ 公告内容……                                 │
│ │                                            │
│ ● 2026-09-15 18:30                            │
│ │ 公告内容……                                 │
│                                              │
│                    [关闭公告] [今日关闭]       │
└──────────────────────────────────────────────┘
```

- 右上角为关闭按钮；右下角两个按钮固定为“关闭公告”“今日关闭”。正文沿垂直时间轴展示时间、类型/置顶标识和内容。
- 桌面宽度建议约 680–760 px、最大高度约为视口的 80%；长内容仅在正文区域滚动，页头和两个底部按钮持续可见。
- 沿用现有日期格式化、`RichContent` 和主题组件；正文安全渲染，不允许注入脚本。无须新增通用 Dialog 或引入时间轴库。
- 弹窗可查看全部配置公告，不能直接沿用通知 hook 的“前 20 条”截断而无入口查看其余记录。

### 关闭及展示规则（建议默认值）

| 操作或场景                           | 计划行为                                                                         |
| ------------------------------------ | -------------------------------------------------------------------------------- |
| 自动弹出                             | 登录成功后首次进入控制台，有公告且展示开关启用时弹出；登录页不自动遮挡登录流程   |
| 右上角 × / 关闭公告 / Esc / 点击遮罩 | 关闭本次；当前标签页的本次页面会话不因路由切换反复弹出，刷新或下次进入可再次弹出 |
| 今日关闭                             | 持久化至当前浏览器本地日期结束，刷新和重新进入仍不自动弹出；次日首次进入恢复     |
| 当天新增或置顶公告                   | 尊重已选择的“今日关闭”，不强制打断用户                                           |
| 手动查看                             | 通知入口提供“查看全部公告”，始终可以重新打开，不受今日关闭限制                   |
| 无公告、展示关闭或读取失败           | 不弹空 Dialog；失败保留正常页面功能及可见重试入口                                |
| 本地存储不可用                       | 降级为内存中的本次关闭；明确提示今日关闭无法跨刷新保存，不影响使用               |

复用现有 `notification-store`，只持久化必要日期/标识；如调整结构，兼容既有存储。该设置按浏览器生效，不新增跨设备账户偏好表。“已读”与“今日关闭”分别处理，不将关闭动作等同于阅读所有公告。

### 验收

- [ ] 管理员可增改删并切换置顶，保存后公开展示同步，旧公告无数据丢失。
- [ ] 时间轴排序正确；长文本、100 条记录、深浅主题下布局正常。
- [ ] 右上及右下按钮符合要求；Esc、遮罩、焦点圈定与关闭后的焦点恢复正常。
- [ ] 普通关闭、今日关闭、刷新、跨日、新增公告及手动重开符合上表。
- [ ] 存储失败、请求失败、空列表、非法输入均有合理行为；普通用户无法改配置。

## 5. 关于页面美化与推荐内容

### 版式

采用与现有站点一致的居中介绍页：顶部标题区、三张服务卡片、三步接入流程、服务与隐私说明、帮助入口。复用 `PublicLayout`、`Card`、`RichContent`、现有按钮和主题变量。

| 区域       | 推荐内容                                                                                       |
| ---------- | ---------------------------------------------------------------------------------------------- |
| 标题区     | 现有站点名称；副标题“面向开发者的 API 接入服务”；按钮“查看接入文档”“浏览模型”                  |
| 服务卡片一 | **统一 API 接入**：通过统一入口接入站点已开放的模型，具体协议与可用范围以文档及模型列表为准    |
| 服务卡片二 | **账户与用量管理**：管理 API Key，查看余额、调用记录与用量统计                                 |
| 服务卡片三 | **清晰的接入指引**：通过文档完成配置，通过公告了解维护及服务变更                               |
| 接入步骤   | 注册/登录账户 → 创建 API Key → 按文档配置并发起请求；若站点关闭注册，提示联系管理员获取账户    |
| 服务边界   | 提供 API 接入与转发服务；可用模型、价格和限制以站内实际配置为准                                |
| 隐私说明   | 摘要引用第 3 节：不售卖用户数据、仅提供 API 服务、监测边界及用户依法使用责任，并链接到完整协议 |
| 帮助区域   | 配置后的文档链接、系统公告、已配置的支持联系方式；没有联系方式则隐藏该项                       |
| 页底       | 保留现有 New API、QuantumNous、许可证和项目归属信息                                            |

不编造在线用户数、SLA、响应速度、认证资质或固定价格，也不增加虚构客服地址。

### 现有内容兼容

- 继续由 `About` 配置和 `GET /api/about` 提供管理员内容，入口仍在“站点 → 系统信息”。
- 空内容使用完整默认介绍；普通文本/Markdown 在新页面中作为“站点介绍”正文展示，使现有一行文本也有合理版式。
- 现有自定义 HTML 和 URL iframe 保留原有隔离及 sandbox 模式，不破坏管理员定制页面，也不把推荐模板覆盖到配置中。
- 文档和模型按钮遵守既有导航开关及访问权限；加载、错误状态复用项目组件，不把请求失败误认为管理员未配置。
- 以 PC Web 的 1440×900、1280×720 及深浅主题为主要视觉验收；较窄窗口检查不溢出，不扩展为独立手机端改版。

### 验收

- [ ] 默认页面具有完整内容层级；一行文本和长 Markdown 都有清晰展示。
- [ ] HTML、URL 自定义模式、主题和现有归属信息不回退。
- [ ] 文档/模型/公告入口可用，没有虚构信息或不实隐私承诺。
- [ ] 键盘、加载/错误状态、正文和长链接溢出检查通过。

## 6. Header 文档链接可配置

- 在“系统公告”页面增加“文档链接”设置区域，复用现有 `general_setting.docs_link`，保留已保存值；不新增含义重复的配置键。
- 原计费/额度设置中的编辑入口移至此处，原位置可保留导航提示，避免两个表单各维护一份草稿。
- 沿用 `PUT /api/option/` 保存，`GET /api/status` 返回 `docs_link`，登录前后 Header 共用 `useTopNavLinks`。
- 前后端校验完整 `http://` 或 `https://` URL，拒绝 `javascript:`、`data:`、无效地址和带用户名/密码的 URL；仅存储链接，不由服务端抓取该地址。
- 保存成功后立即失效 status 查询和旧本地缓存；Header 下一次点击使用新地址。后台保存失败时不显示成功或丢失输入。
- 继续以新标签页打开外部链接，保留 `noopener noreferrer`。导航显示开关仍由现有 Header 配置控制。
- 清空时建议恢复现有默认文档地址 `https://docs.newapi.pro`，界面提示“留空使用默认文档”。当前 Header 的空值回退是 `/docs`，但路由目录未发现对应页面，因此实施时需统一空值行为并验证默认地址有效，避免死链接。
- 首页文档按钮也使用同一 `docs_link`，关于页的新文档按钮同样复用；在设置说明中告知这是共享地址。

验收：保存后无需强制刷新即可跳到目标 URL；刷新后配置保持；登录前后 Header 一致；旧值无丢失；非法 URL 被后端拒绝；无权限用户不可修改；空值行为有回归覆盖。

## 7. 推荐计划：逐笔等待 48 小时后手动提现

### 需要先明确“提现”的含义

当前系统只有邀请奖励转入**站内可用余额**的功能。本计划建议先按这一含义实施，并在弹窗中明确“提取至站内可用余额”。

如果期望提现到支付宝、微信或银行卡，需要补充收款方式、提现申请、审核、实际打款、手续费、失败/退款和对账规则；在用户确定前，不把站内余额划转当作现金提现交付。

### 用户可见行为

- 在钱包推荐计划保留邀请码、邀请链接及复制功能，增加明确的“提现”按钮、冻结金额、可提现金额和奖励明细。
- 每笔奖励显示金额、产生时间、可提现时间以及“冻结中 / 可提现 / 已提现”状态；保留部分提现时还需显示剩余金额。
- **每一笔奖励从自身成功产生的服务端时间起独立计算 48 小时**：`available_at = created_at + 172800 秒`，不是注册账号统一计时，也不是第一次提现后统一计时。
- 未到期金额不允许提现；到期仅改变资格，不自动划转。刷新、登录、页面停留、定时任务均不得触发自动提现。
- 主按钮建议提取用户选择金额范围内的已到期奖励，默认填入全部可提金额；明细逐笔计时，扣减顺序固定为到期时间、记录 ID 升序。
- 没有可提金额时按钮禁用并显示最近一笔的可提现时间。倒计时仅作展示，前端根据服务端返回的当前时间校准，每次提交仍由后端重新判定。
- 复用现有 `TransferDialog`，显示提现去向、金额、最低额度说明及确认按钮；提交中防重复点击，成功刷新余额/明细，失败保留可重试状态。

示例（北京时间展示，底层使用 UTC 时间戳）：

| 奖励 | 产生时间         | 可以手动提现的最早时间 | 9 月 18 日 10:30 的状态  |
| ---- | ---------------- | ---------------------- | ------------------------ |
| A    | 9 月 16 日 10:00 | 9 月 18 日 10:00       | 可提现，但不会自动到账   |
| B    | 9 月 16 日 12:00 | 9 月 18 日 12:00       | 冻结中；A 的提现不影响 B |

### 账本、接口与原子性

- 当前 `User.AffQuota`、`AffHistoryQuota` 只有汇总，无法支持逐笔时间。新增邀请奖励账本，至少记录：归属用户、唯一奖励来源、奖励类型、整数额度、剩余额度、产生时间、可提时间及提现记录关联。
- 奖励来源使用被邀请用户和奖励类型等稳定标识去重；普通注册、OAuth 注册及回调重试只能发放一次。邀请人奖励 `QuotaForInviter` 纳入冻结，受邀人注册赠送 `QuotaForInvitee` 不顺带改变。
- 奖励写入、邀请计数及汇总余额必须保持原子一致，失败显式返回或进入可恢复流程，不能继续忽略发奖错误；兼容现有普通/OAuth 用户创建事务边界。
- 新增读取当前用户奖励汇总/分页明细的能力，返回冻结额、可提额、服务端时间和逐笔时间；接口不能读取他人奖励。具体路由在实现前与现有 wallet API 一并定稿。
- 保留 `POST /api/user/aff_transfer` 路径，接入相同的成熟奖励校验与扣账逻辑；不能让旧接口绕过新按钮的 48 小时限制。为保证提交重试安全，写请求需携带幂等请求标识；缺少该标识的旧客户端应收到明确的升级提示且不扣账，并在接口变更说明中记录。
- 每次提现在同一事务内校验用户归属、到期时间、剩余额度、最低额度、钱包上限，扣减对应奖励并增加钱包余额，写入唯一提现请求标识及分配明细。重试应返回同一结果，不能重复记款。
- 沿用整数 quota 和项目钱包上限 `common.MaxWalletQuota`；拒绝零、负数、超额和越界值。保留付款条款检查、身份校验及关键操作限流。
- 通过 `lockForUpdate(tx)` 兼容 MySQL/PostgreSQL；SQLite 不支持该锁语法，必须结合条件更新、受影响行数和事务冲突处理验证一致性，不能把用户行锁当作全部防重措施。
- 事务提交后同步或失效余额缓存；记录不含认证凭据的提现审计。若日志使用独立数据库，不假设主库交易和日志库具有跨库事务，需验证失败记录/补记方式不会引发重新入账。

### 历史数据与现有规则

1. **历史余额**：旧汇总不能可靠恢复每笔产生时间。推荐将迁移前剩余 `aff_quota` 生成唯一“历史结转”记录并保持可提，之后的新奖励逐笔冻结 48 小时。该做法是明确的历史例外，需要实施前确认；如要求所有旧余额也等待 48 小时，应明确从迁移时刻重新起算，不能伪造历史逐笔时间。
2. **最低额度**：目前模型层最低转入量为 `common.QuotaPerUnit`，默认 500000 quota（一个基础货币单位）。推荐保留并明确提示；48 小时到期不等于自动取消最低额，也不能按显示币种误算门槛。
3. **部分提现**：推荐保留旧的自选金额行为，对已成熟记录按固定顺序扣减；明细允许部分剩余，未到期金额不被挪用。若决定改成只能提现整笔或全额，应先明确旧客户端兼容策略。
4. **可核对性**：迁移前后剩余推荐余额一致，新账本剩余额度合计与汇总对应，累计历史奖励不因迁移或提现重复增加；迁移唯一键确保重复启动不生成第二笔结转。

### 迁移、备份与回滚

这是涉及钱包、数据格式和公开接口的高风险实现阶段，实施前先确认以上三项业务规则，备份用户余额、奖励汇总、相关配置及独立日志库，并保存对应应用版本。

- 采用新增账本的兼容迁移，先在隔离数据上验证，再按授权的维护窗口迁移；不删除旧汇总字段，不重复给钱包加钱。
- 对真实 SQLite、MySQL、PostgreSQL 分别执行新建数据库、代表性旧版本升级、重复两次启动、历史结转与并发提现验证；基线同时覆盖上游最新发布版本和当前 fork 的实际结构。
- 记录准确数据库版本、命令、结果和前后余额对账；涉及版本特定行为时加测项目最低支持版本，日志库独立配置也要覆盖。
- 回滚先暂停奖励/提现写入并对账。旧程序的 `/aff_transfer` 不识别冻结账本，不能直接退回旧二进制继续放款；优先使用保留冻结校验的兼容修复版本。恢复旧备份必须处理迁移后的真实交易，不能抹掉已发生的资金变动。

### 验收

- [ ] 47:59:59 拒绝，48:00:00 允许；改变浏览器时间不能提前提现。
- [ ] 多笔不同时间分别解冻；A 提现不影响 B；到期后不点击不会到账。
- [ ] 普通注册、OAuth 注册及重复回调只产生一份奖励，受邀人赠送规则保持一致。
- [ ] 连点、网络重试、相同请求重放和两个会话并发只产生一次有效入账；失败整笔回滚。
- [ ] 旧接口不能绕过等待期；越权、负数、零、超可提额、最低额和钱包上限都有覆盖。
- [ ] 部分提现、刷新后明细/余额/缓存一致；历史结转幂等且可对账。
- [ ] 真实三数据库及独立日志库所需验证通过；环境变量缺失导致的跳过不算通过。

## 8. 实施顺序、检查与交付

### 阶段清单

- [x] 完成五项需求及现有实现调查，核实可复用组件、配置和接口。
- [x] 形成此计划，区分建议、兼容边界和未确认的业务规则。
- [x] 按用户补充细化登录勾选文案与完整协议，写明数据不售卖、API 服务范围、监测边界及违法使用责任。
- [ ] 阶段 A：确定提现去向、历史余额规则、最低额度/部分提现规则及可发布的协议表述；补采当前测试和数据处理基线。
- [ ] 阶段 B：整合系统公告设置入口、复用文档 URL 配置并修复缓存刷新，完成置顶与时间轴弹窗。
- [ ] 阶段 C：核对实际数据处理与协议正文，修正与最终声明冲突的路径，再实现登录协议确认；随后完成引用同一说明的关于页面。
- [ ] 阶段 D：在确定的提现方案下完成账本、幂等发奖、成熟奖励提现、历史迁移及钱包界面。
- [ ] 阶段 E：完成专项回归、三数据库验证、桌面操作冒烟及最终差异审查，更新本文件的证据和剩余事项。

P0 表示必须守住的正确性和发布门槛；界面可以分阶段完成，不能在 P0 验证缺失时把整体需求标为完成。

每个阶段记录修改文件、最后成功命令、结果、阻塞项和下一步。若并行开展，公告/关于页、认证、钱包按模块分工，共享的状态类型、路由、配置保存 hook 和翻译文件由主任务串行整合，避免并发覆盖。

### 实施后的验证要求

- 前端：先复用项目业务组件；补充受影响模块 `__tests__/` 下的用户行为测试，覆盖公告关闭/跨日、设置保存刷新、协议禁用/绕过、关于页内容模式、奖励冻结/提交反馈。
- 国际化：实施文案前读取 `i18n-translate` 技能，使用英文源字符串键，补齐 en、zh、zh-TW、fr、ja、ru、vi；管理员输入的公告原文不自动翻译或改写。
- 前端命令：在 `web/` 执行 `bun run test`（先相关目录再完整范围）、`bun run typecheck`、受影响文件的 oxlint/格式检查、`bun run build`；真实脚本以 `web/package.json` 为准。复用已验证的本地 Bun 入口，缺依赖时才按锁文件安装。
- 后端：优先扩展现有合适测试；推荐奖励若需新文件，将主要业务用例集中在一个文件，使用 `require`/`assert`。执行相关测试及 `go test ./...`、`go vet ./...`、`go build ./...`；先确保所需 `web/dist` 存在。
- 数据库：凡公告数据格式、配置持久化、奖励账本或迁移受影响，按仓库规范在真实 SQLite/MySQL/PostgreSQL 上验证对应存取/迁移路径；新增账本还需新库、升级、重复启动和并发矩阵。不得以 mock、单 SQLite 或构建通过代替。
- 桌面冒烟：登录确认 → 控制台公告 → 今日关闭/重开；管理员增改公告/置顶 → 用户查看；修改文档 URL → Header 跳转；关于页；奖励从冻结到手动提现。每条流程至少实际操作一次，检查浏览器控制台错误和深浅主题。
- 若触及 `relaykit/`，额外执行独立模块的 `GOWORK=off go build ./...`；未涉及则不扩展修改范围。
- 提交前检查完整 diff、Git 状态、凭据/调试输出及生成噪声。后续实现属于跨模块、含钱包迁移的大变更，验证完成后按仓库规则提交中文 Conventional Commit、创建中文说明的 annotated Tag 并推送 fork 对应分支及 Tag；生产发布另行确认。

### 认证设计参考

本次核查到的最新稳定 ASVS 为 **5.0.0**（GitHub 的 `latest` 滚动构建不作为稳定版本）。实施时重新核对最新稳定版，根据实际改动映射适用条目 ID，并在验证记录中保留引用和结果；本计划不构成合规认证。

- [OWASP ASVS 5.0.0 稳定发布](https://github.com/OWASP/ASVS/releases/tag/v5.0.0_release)
- [Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)：统一失败信息、认证保护及不含凭据的审计。
- [Session Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html)：会话轮换、失效和服务端状态校验。
- [CSRF Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)：协议确认、配置写入和提现等状态变更请求的服务端防护。
- [OAuth 2.0 Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/OAuth2_Cheat_Sheet.html)：在改动 OAuth 发起与回调流程前核对 state、PKCE 等适用控制。

### 当前检查点

当前完成的是需求计划与代码现状核对。应用代码、配置、余额和数据库均未更改；各功能验收仍为待执行。下一步从阶段 A 的业务规则和验证基线开始，不能从历史提交或此文档推断功能已经完成。

2026-09-16 协议补充基线：`68e1ce6da6a541770787382639e8f2d2001dd77c`，`main` 跟踪 `fork/main`，开始时工作区干净。本次仅更新第 3 节协议及对应摘要、验收和检查点，登录界面和实际数据处理行为仍待实施。

文档验收：`oxfmt --check next.md` 通过，23 处本地文件引用均存在，文档差异的空白检查通过，常见凭据模式检查无命中。应用 lint、类型检查、测试、构建和数据库验证不属于本次文档交付结果，需在后续实现时执行。
