# rc.37 定制兼容升级检查点

## 目标与基线

- 本次为 P0 兼容升级，固定官方 `v1.0.0-rc.37` (`385d2dfd10d821b25c8a6766bd16eea248cb1652`)，不合并发布后的 main。
- fork 基线为 `main@6e44f3d990aa700958e8f8cf88049639293ba455`、Tag `v1.0.0-rc.35.custom.12`，工作区干净，远端 `fork/main` 相同。
- 隔离工作区为 `D:/newapi/.local-tests/worktrees/upgrade-rc37`，分支 `codex/upgrade-rc37`。主工作区保留原版本，现有预览服务不切换。
- 运行时：Go 1.26.0、Node 24.12.0、Bun 1.4.1（已存在的 npm 本地缓存）、Docker 29.7.2。按上游 `web/bun.lock` 安装；Go 模块依赖与当前 fork 相同。
- 基线证据：上一轮同一提交的前端 113 文件/860 测试、后端 44 测试包、build/vet/typecheck 和三库公告专项已通过，见 `next.md` 第 13 节及 `.local-tests/next-acceptance`。本轮合并后重新执行检查。

## 必须保留的行为

- 公告确认后立即持久化、失败保留输入、取消旧查询、自动与手动弹窗、旧 Notice 不混入时间轴。
- 登录主动同意、协议 Dialog 阅读、注册无需同意且不建立会话、各备用入口及二次验证保留服务端门禁；QQ 邮箱发码提示。
- 推荐奖励逐笔 48 小时冻结、主动转入站内余额、幂等重试及历史账本；已有余额和配置不丢失。
- 关于页、文档链接入口、兑换码首行/空白匹配与复制/批删、API 地址展示复制、Sub2 耗时颜色。
- Responses 路径和工具/流式兼容、推理档位计费、管理员价格覆盖及显式零值；视频延续现行 rc.35 插件契约，不恢复已淘汰的旧视频实现。
- New API / QuantumNous 归属、现有部署端口/域名/卷配置。

## 风险与回滚

涉及定价、Passkey RP ID、配置主键及数据库迁移。验收只使用新建隔离库和模拟供应商，不操作生产、不调用付费 API。真实 SQLite、MySQL、PostgreSQL 均验证空库、当前 fork 与官方 rc.37 数据库升级，共享/独立日志库，重复启动及数据/索引/约束保持。

生产上线需单独安排维护窗口、停止写入并备份主库/日志库、配置与镜像，验证备份可恢复。回滚使用配套旧程序、配置与备份，不让旧程序直接写入未经验证的新 schema。当前 Tag 保留为代码回退点。

## 阶段

- [x] 核对本地与远端基线、官方 Tag、现有定制及项目规则。
- [x] 建立隔离工作区并启动无提交合并；初始 46 个冲突文件。
- [x] 清除全部冲突并复核定制；三个 sub-agent 已交接，主 agent 完成最终暂存整合。
- [x] 前端全量及新增粘贴测试、typecheck/构建、修改文件 lint/format，后端全量/build/vet、relaykit 独立构建测试；全仓既有 lint 债务见报告。
- [x] 三数据库迁移与业务专项、真实浏览器关键流程、最终 Windows/Docker 产物及 HTTP 验收。
- [x] 完整 diff、敏感信息和检查点复核，准备中文合并提交与 annotated Tag 的交付信息。

## 分工

- 主 agent：认证前后端、语言文件结构化整合、依赖、集成验收、文档与 Git 交付。
- announcement_review：公共 Header、公告及其他设置、兑换码、钱包 hooks、HTTP/Query 错误处理及相关前端回归。
- option_persistence：model/ 冲突及事务/迁移兼容、真实三库矩阵、数据库报告。
- release_baseline：计费表达式、价格覆盖、任务插件与定价编辑器兼容及集中回归。

## 已核实的验收进展

- 公告真实浏览器验收 10/10 通过，包含保存、编辑、置顶、删除、失败重试、刷新、重启与 Notice 来源隔离；控制台及页面错误为空。
- 三库 18 个迁移组合、36 次启动、六组提现接口通过；模型回归 20 个顶层测试通过。详见 `database.md`，证据标识 `20260917180815254`。
- 三库验收发现并修复冻结奖励重启重复结转、受邀赠送被误计入可提现账本、配置主键迁移失败继续启动的问题；保留原始数据，不自动纠正既有历史异常账本。
- 前端首次全量的失败已逐项处理：包括执行负载下的超时、Passkey 测试缺少 QueryClient、关于页测试选中导航而非 iframe、旧定价矩阵断言。最终全量 154 文件/1655 通过；随后新增粘贴 4 项通过，钱包 8 文件/23 测试通过。
- Passkey/登录协议专项、安全页面测试、关于页 13 项测试、controller 管理员显式价格覆盖测试已通过。
- 最终 `bun run build:check`、Go 44 个测试包/build/vet、relaykit 独立 build/test 和 Docker 构建均通过。
- 完整应用 HTTP 39/39、最终产物公告浏览器 10/10、核心浏览器 14/14 通过。新增奖励、兑换码原生粘贴和缓存模态转换缺陷均先复现再修复。
- 定价与 Passkey 控制器三库专项补齐：定价 154 条 PASS，Passkey 每库 14 个顶层测试/46 条 PASS，无跳过，证据标识 `20260917183610830`。
- 全量 lint 当前 192 个 error，官方基线 194、fork 基线 298；无新增诊断。手工兼容改动单独 lint/format 通过。

最后成功命令：最终源码 `GOWORK=off go test ./... -p 1 -count=1 -timeout=300s`、`go build ./...`、`go vet ./...` 均退出 0。最终产物、完整命令与限制见 `upgrade.md`。本检查点随升级合并提交交付，Tag 固定为 `v1.0.0-rc.37.custom.1`，目标为 `fork/main`；主工作区以 fast-forward 接收，远端提交及 Tag 必须由最终 `git ls-remote` 输出核验。

本地新版预览为 `http://127.0.0.1:3022/about`，Windows 产物 SHA-256 为 `6982307aca447d4c3b5da7130482d7364784dfa227a967334efbf927fad1d121`。3021 旧预览未切换。生产备份、真实外部身份和模型联调仍按既有门槛另行安排。
